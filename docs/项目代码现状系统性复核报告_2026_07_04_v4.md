# PTM2CellNet 项目代码现状系统性复核报告 v4

**复核日期**：2026-07-04（第四轮）  
**复核版本**：`main@a3caf00`（自上一份报告 `3c9d21d` 后新增 6 个变更文件，+1085/-15 行）  
**复核范围**：`src/`（85 非 `__init__`，31087 行）、`tests/`（105 文件，1470 测试函数）、`scripts/`（25 文件）、`configs/`（28 YAML）、依赖文件及 Docker 配置。  
**分析方法**：CodeGraph 结构索引（257 文件，5326 节点，6508 边）+ 全量测试 1470 项 + mypy 静态分析 + 手动审查  
**参考基线**：
- `docs/项目代码现状系统性复核报告_2026_07_04_v3.md`
- `docs/项目代码现状系统性复核报告_2026_07_03_v2.md`
- `docs/项目代码现状系统性复核报告_2026_07_03.md`
- `docs/code_status_comprehensive_review_2026_06_30.md`

---

## 1. 总体结论

1. **工作目录干净**：`main@a3caf00`，已提交未合并更改，无需合并操作。
2. **测试规模**：1470 个测试函数（+13 自上一轮），105 个测试文件（0 变化），**1464 通过，6 跳过**。
3. **安全完备**：6 项安全审计全部通过（认证/CORS/速率限制/safe_torch_load/路径校验/Docker 非 root）。SEC-06（`initialize.py` 使用 `!=`）已修复。
4. **新增功能**：`ESM3Encoder` 类（F-01 解决）、`PTMVirtualPerturbationEngine` 及管线（F-06 解决）。
5. **阻塞性问题**：**无**。Dockerfile 引用的 `outputs/models/best_model.pt` 等模型文件**已存在**。
6. **整体成熟度**：核心架构 ~96% 完备，类型安全仍是主要技术债（8 文件 `ignore-errors`、91 处 `Any`）。

---

## 2. 未实现功能项

### 2.1 本轮已解决项（相对于 v3 报告）

| 项 | 解决方式 | 对应提交 |
|---|---|---|
| F-01：ESM-3 集成 | `ESM3Encoder(PretrainedEncoder)` 类实现，多模态输入（sequence/structure/function tokens），3 种模型尺寸，LoRA 支持 | `a3caf00` |
| F-06：PTM 虚拟扰动流水线 | `PTMVirtualPerturbationEngine` + `PTMPerturbationPipelineConfig` + `create_perturbation_pipeline()` 工厂函数，端到端编排 | `a3caf00` |
| G-01/DEPLOY-01：Docker COPY 缺失模型文件 | **自动解决**——模型文件已存在于 `outputs/models/`（`best_model.pt`、`best_model.config.yaml`、`artifact_manifest.json`） | 外部变更 |
| SEC-06：`initialize.py` 使用 `!=` | `hmac.compare_digest` 已应用 | 外部变更 |

### 2.2 仍存在的未实现功能项

| ID | 类别 | 未实现项 | 严重程度 | 位置/说明 |
|---|---|---|---|---|
| F-02 | **模型** | 质谱流式数据流式处理 | 低 | `roadmap.py:V2-02` — `mass_spec_stream()` 解析制表符记录为 DataFrame，无流处理基础架构 |
| F-03 | **模型** | 自定义 PTM 数据库 | 低 | `roadmap.py:V2-03` — `load_custom_ptm_database()` 功能完整但无数据库后端加载器 |
| F-04 | **训练** | 分布式训练（DDP/FSDP）集成 | 中 | `roadmap.py:V2-04` — `distributed_trainer()` 包装 Lightning DDP 但未集成到训练脚本 |
| F-05 | **前端** | GUI 界面 | 低 | `roadmap.py:V2-05` — `launch_gui()` 写入 12 行 Streamlit 占位应用 |
| F-07 | **模型** | ESM2 模型包装器简化版 | 低 | `scripts/ensemble_predict.py:88-120` — `_load_esm2_model()` 有返回零值的 dummy forward |
| F-08 | **训练** | `train_binary.py` 硬编码标签 | 低 | `label_map = {"Quiescent": 0, "Activated": 1}` — 非配置驱动 |
| F-09 | **文档** | Sphinx 文档未构建 | 中 | `docs/conf.py` 存在但 `sphinx` 未安装；**无 `docs/index.rst`** |
| F-11 | **CI** | Dependabot 未配置 | 中 | 无 `.github/dependabot.yml` |
| F-12 | **CI** | 安全扫描缺失 | 中 | 无 CodeQL / pip-audit / Safety / Trivy |
| F-13 | **可重现** | 无锁定文件 | 中 | 所有 `requirements-*.txt` 使用 `>=` 范围 |
| F-14 | **文档** | CONTRIBUTING.md 缺失 | 中 | 开源项目必备 |
| F-15 | **CI** | CI 无安全扫描 | 中 | `pip-audit`/`safety`/`trivy` 均未配置 |

**引用**：[F-02] `src/models/roadmap.py:175`；[F-04] `src/models/roadmap.py:234`；[F-07] `scripts/ensemble_predict.py:88`；[F-08] `scripts/train_binary.py:47`；[F-09] `docs/conf.py`（无 `index.rst`）

### 2.3 部分实现功能

| 功能模块 | 完成度 | 缺失组件 |
|---|---|---|
| 安全加固 | 100% | 全部 6 项安全审计通过 |
| 类型安全 | 72% | 8 文件仍完全跳过 mypy；91 处 Any（尤其是 `genki_adapter.py` 21 处） |
| 架构清理 | 65% | `WindowedSequenceProcessor` 已提取但 `long_sequence.py`/`pretrained_encoders.py` 未使用（死代码） |
| 文档 | 30% | Sphinx 骨架就位但 `docs/index.rst` 缺失，`sphinx` 未安装 |
| CI | 20% | 基础 lint+test CI 存在，无 Dependabot/代码扫描/构建验证 |

---

## 3. 技术债分类梳理

### 3.1 类型安全 — 严重程度：**高**

#### 3.1.1 `mypy: ignore-errors` 完全跳过（8 文件）

| 文件 | 行数 | 核心内容 | 风险 |
|---|---|---|---|
| `src/models/long_sequence.py` | 985 | Mamba SSM + 长序列逻辑 | **高** |
| `src/models/encoders.py` | 762 | CNN/Transformer/LSTM/GRU 编码器 | **高** |
| `src/models/pooling.py` | — | 池化策略（所有编码器共用） | 中 |
| `src/models/multitask_ptm.py` | — | 多任务 PTM 预测器 | 中 |
| `src/models/model_utils.py` | — | 批量大小估算、模型摘要 | 中 |
| `src/data/multitask_dataset.py` | — | 多任务数据集逻辑 | 中 |
| `src/analysis/variant_workflow.py` | — | 变体分析工作流 | 中 |
| `src/utils/sequence_utils.py` | 219 | WindowedSequenceProcessor（死代码） | 低 |

**引用**：[TC-01] `src/models/long_sequence.py:1`；[TC-02] `src/models/encoders.py:1`

#### 3.1.2 `mypy: disable-error-code` 部分跳过（11 文件）

全部抑制相同的 5-6 个错误码（`arg-type,assignment,dict-item,operator,return-value,name-defined`），表明是系统性模式：

| 文件 | 抑制码数 |
|---|---|
| `src/data/augmentation.py` | 6 |
| `src/data/ptm_site_dataset.py` | 6 |
| `src/models/signaling_network.py` | 6 |
| `src/models/variant_effect.py` | 6 |
| `src/models/multitask.py` | 6 |
| `src/models/ptm_site_predictor.py` | 6 |
| `src/models/architectures.py` | 5 |
| `src/analysis/pathway_integration.py` | 5 |
| `src/training/peft_config.py` | 6 |
| `src/api/routes/state.py` | 4 |
| `src/api/routes/predictions.py` | 4 |

**引用**：[TC-03] `src/data/augmentation.py:6`

#### 3.1.3 `type: ignore` 局部抑制 — 28 处

主要集中：
- `src/training/callbacks.py` — 9 处（Lightning Callback 重载模式）
- `src/integration/__init__.py` — 5 处（可选依赖桩）
- `src/api/app.py` — 3 处（Starlette `add_middleware` 重载）

**引用**：[TC-04] `src/training/callbacks.py`（9 处）

#### 3.1.4 `: Any` 标注 — 91 处

高密度分布：
- `src/integration/genki_adapter.py` — **21 处**（最高优先级，↑4 自上一轮）
- `src/utils/io.py` — 9 处
- `src/models/scvi_adapter.py` — 9 处
- `src/evaluation/explainers.py` — 7 处
- `src/models/roadmap.py` — 6 处
- `src/utils/config.py` — 5 处
- `src/training/peft_config.py` — 5 处

**引用**：[TC-05] `src/integration/genki_adapter.py`（21 处 Any，增长 4 处）

#### 3.1.5 mypy 当前报错 — 30 处（7 文件）

| 问题类型 | 数量 | 主要位置 |
|---|---|---|
| `no-any-return` | 1 | `ptm_virtual_perturbation.py:194` |
| `var-annotated` | 20 | `analysis/__init__.py` 和 `evaluation/__init__.py` |
| `union-attr` | 1 | `architectures.py:427` |
| `misc`（None not callable） | 2 | `architectures.py:431`、`predictions.py:370` |
| `misc`（Cannot assign to type） | 2 | `state.py:18,27` |
| `unused-ignore` | 2 | `predictions.py:217,218` |
| `call-arg` | 1 | `predictions.py:621` |

**引用**：[MYPY-01] mypy 输出，30 errors in 7 files

#### 3.1.6 解决策略

```
阶段 1（1-2 天）：
  - 将 long_sequence.py + encoders.py 的 ignore-errors 改为 disable-error-code
  - 为 genki_adapter.py 的 21 处 Any 引入类型桩或 Protocol 类
  - 修复 30 个 mypy 报错（大部分是 var-annotated，只需在 __init__.py 添加类型标注）

阶段 2（2-3 天）：
  - 为 pooling.py / multitask_ptm.py 添加完整类型标注
  - audit 28 处 type: ignore 并移除可修复项
  - pyproject.toml 添加 [[tool.mypy.overrides]] 集中管理 11 文件的抑制码
```

### 3.2 代码质量 — 严重程度：**中**

#### 3.2.1 大文件需拆分（>500 行，20 个）

| 排名 | 文件 | 行数 | 建议 |
|---|---|---|---|
| 1 | `src/models/davf.py` | **985** | 拆为 davf_core / davf_loss / davf_config |
| 2 | `src/data/loaders.py` | **908** | 按数据源拆分（uniprot / phosphosite / dbptm / cplm） |
| 3 | `src/data/homology_splitter.py` | **902** | 拆为 homology_features / clustering / splitter |
| 4 | `src/api/app.py` | **857** | 拆为 app_factory / middleware / monitoring |
| 5 | `src/models/pretrained_encoders.py` | **821** | ESM2/ESM3/ProtBERT/ProtT5 可独立（↑自 v3 的 525 行，因 ESM3Encoder 新增） |
| 6 | `src/utils/io.py` | **818** | 拆为 io_safe / io_torch / io_hdf5 / io_format |
| 7 | `src/data/extended_pathway_kb.py` | **775** | 数据定义可独立为 YAML 文件 |
| 8 | `src/data/features.py` | **762** | 拆为 features_sequence / features_ptm / features_selection |
| 9 | `src/models/external_tools.py` | **692** | 按工具拆分 |
| 10 | `src/api/routes/predictions.py` | **667** | 按端点拆分 |

**引用**：[ARCH-01] `src/models/davf.py`（985 行）；[ARCH-02] `src/models/pretrained_encoders.py`（821 行，↑296 行）

#### 3.2.2 死代码

| 文件 | 行数 | 问题 | 严重程度 |
|---|---|---|---|
| `src/utils/sequence_utils.py` | 219 | `WindowedSequenceProcessor` — 全项目无人导入、无人使用 | **高** |
| `src/utils/lazy_import.py` | 42 | `LazyImport` — 仅在 `evaluation/__init__.py` 和 `analysis/__init__.py` 中间接使用，无隔离单元测试 | 中 |

**引用**：[DEAD-01] `src/utils/sequence_utils.py`（219 行死代码）

#### 3.2.3 `except Exception:` 捕获 — 7 处（其中 1 处静默 pass）

| 文件 | 行号 | 风险 |
|---|---|---|
| `src/models/external_tools.py` | 164 | **`except Exception: pass`** — 静默吞噬异常 |
| `src/integration/genki/reference_data.py` | 314 | 无日志 |
| `src/models/model_utils.py` | 201 | 无日志 |
| `src/training/artifacts.py` | 209 | 哈希计算失败静默 |
| `src/api/app.py` | 409 | 仅设置标志 |
| `src/utils/dependency_check.py` | 95 | 依赖检查失败 |
| `src/models/davf_checkpoint_utils.py` | 182 | checkpoint 加载失败 |

**引用**：[EXC-01] `src/models/external_tools.py:164`（静默 pass）

#### 3.2.4 硬编码魔法值

| 位置 | 值 | 说明 |
|---|---|---|
| `src/models/davf.py` | 256, 512, 128, 50 | 架构维度 + ODE 步数 |
| `src/models/latent_davf.py` | 256, 512 | DAVF 匹配维度 |
| `src/models/biperturb.py` | 256, 256 | 架构维度 |
| `src/models/external_tools.py` | 100.0, 1.0, 50, 50 | 置信度阈值、BLAST 命中限制 |
| `src/models/pretrained_encoders.py` | 1024, 1022, 100 | 最大 token 长度、窗口大小 |

**引用**：[MAGIC-01] `src/models/davf.py`（多处）

### 3.3 测试覆盖 — 严重程度：**中**

#### 3.3.1 总体测试数据

| 指标 | 数值 |
|---|---|
| 测试函数总数 | 1470（+13 自 v3） |
| 通过 | 1464 |
| 跳过 | 6 |
| 测试文件数 | 105 |
| 覆盖率（源文件级） | 91.8%（78/85） |

#### 3.3.2 `pragma: no cover` — 13 处

主要分布：
- `src/models/roadmap.py` — 4 处（可选依赖兜底，合理）
- `src/models/scvi_adapter.py` — 2 处
- `src/integration/genki/reference_data.py` — 2 处
- `src/utils/io.py` — 1 处

全部有正当理由（可选导入兜底、环境依赖路径），但应在重构时逐步消除。

**引用**：[COV-01] `src/utils/io.py:23`

### 3.4 安全 — 严重程度：**低**（全部已修复）

| ID | 问题 | 状态 |
|---|---|---|
| SEC-01~05 | 安全加固（中间件/safe_torch_load/CORS/路径校验/Docker） | ✅ 已修复 `3c9d21d` |
| SEC-06 | `initialize.py` 使用 `!=` 而非 `hmac.compare_digest` | ✅ 已修复（外部变更） |
| SEC-07 | CI 无安全扫描（CodeQL/pip-audit/Safety/Trivy） | ❌ 仍缺失 |
| SEC-08 | 依赖无锁定文件 | ❌ 仍缺失 |

### 3.5 文档 — 严重程度：**中**

| ID | 缺失项 | 严重程度 | 说明 |
|---|---|---|---|
| DOC-01 | `docs/index.rst` | **高** | `sphinx-build` 无法启动 |
| DOC-02 | CONTRIBUTING.md | 中 | 开源项目必备 |
| DOC-03 | Model Card | 中 | 无模型用途/限制说明 |
| DOC-04 | Sphinx 未构建 | 中 | `sphinx` 未安装（`python -c "import sphinx"` 失败） |

### 3.6 依赖管理 — 严重程度：**中**

| ID | 问题 | 严重程度 | 说明 |
|---|---|---|---|
| DEP-01 | 无 `requirements-lock.txt` | **高** | 不可重现构建 |
| DEP-02 | 无 Dependabot | 中 | 不感知依赖漏洞 |
| DEP-03 | `torchmetrics` 重复声明 | 中 | `requirements-core.txt`（`>=0.7.0`）+ `requirements-pretrained.txt`（`>=0.11.0`）版本下限不同 |
| DEP-04 | 关键依赖无上限 | 低 | PyTorch 3.0 可能破坏构建 |

### 3.7 CI/DevOps — 严重程度：**中**

| ID | 缺失项 | 严重程度 | 说明 |
|---|---|---|---|
| CI-01 | Docker 构建只在 main 运行 | 中 | PR 合并前不验证 Docker 构建 |
| CI-02 | 无 `Makefile` / `tox.ini` | 低 | 无开发者便捷目标 |
| CI-03 | 无 `pre-commit-config.yaml` | 低 | 无提交前格式化门禁 |
| CI-04 | 无 `.editorconfig` | 低 | 跨贡献者编辑器不一致 |
| CI-05 | 无 `pyproject.toml` mypy overrides | 中 | 8 个 `ignore-errors` + 11 个 `disable-error-code` 文件分散管理 |

---

## 4. 分阶段修复路线图

### 阶段 1：类型安全提升（优先级最高 — 快速见效）

| 任务 | 工作量 | 影响文件 | 风险 |
|---|---|---|---|
| 修复 mypy 30 个错误（7 文件） | 2 小时 | 7 文件 | 低——大部分是 `var-annotated` 只需添加类型标注 |
| `long_sequence.py` + `encoders.py` → `disable-error-code` | 1 小时 | 2 文件 | 低——仍跳过检查但消除顶格抑制 |
| `genki_adapter.py` 21 处 Any → Protocol 类 | 1.5 小时 | 1 文件 | 低——已有 `GenePerturbationRequest` 等契约类 |
| `pyproject.toml` `[[tool.mypy.overrides]]` 集中管理 | 30 分钟 | `pyproject.toml` | 低 |
| 修复 `predictions.py:621` call-arg 缺失 `use_davf` | 5 分钟 | 1 文件 | 低 |

### 阶段 2：文档与 CI（高优先级）

| 任务 | 工作量 | 影响文件 | 风险 |
|---|---|---|---|
| 创建 `docs/index.rst` | 30 分钟 | `docs/index.rst`（新建） | 低 |
| 生成 `requirements-lock.txt` | 10 分钟 | `requirements-lock.txt`（新建） | 低 |
| 配置 Dependabot（`.github/dependabot.yml`） | 15 分钟 | `.github/dependabot.yml`（新建） | 低 |
| 创建 CONTRIBUTING.md | 30 分钟 | CONTRIBUTING.md（新建） | 低 |

### 阶段 3：架构清理（中优先级）

| 任务 | 工作量 | 影响文件 |
|---|---|---|
| 集成 `WindowedSequenceProcessor` 到 `long_sequence.py`/`pretrained_encoders.py` | 1 小时 | 3 文件 |
| 或决定移除 `sequence_utils.py` 死代码 | 5 分钟 | 1 文件 |
| `external_tools.py:164` 移除静默 `pass` | 5 分钟 | 1 文件 |
| `davf.py` 985 行 → `davf_core.py` + `davf_loss.py` | 2-3 小时 | 3-4 新文件 |

### 阶段 4：CI/DevOps（低优先级）

| 任务 | 工作量 | 影响范围 |
|---|---|---|
| CI 添加 pip-audit/CodeQL 安全扫描 | 30 分钟 | `.github/workflows/ci.yml` |
| 创建 `Makefile` | 30 分钟 | `Makefile`（新建） |
| 创建 `.pre-commit-config.yaml` + `.editorconfig` | 15 分钟 | 2 个新文件 |

---

## 5. 指标汇总与对比

### 5.1 与前一报告对比

| 指标 | v3 (`3c9d21d`) | v4 (`a3caf00`) | 变化 |
|---|---|---|---|
| 源文件（非 `__init__`） | 85 | 85 | 0 |
| 测试文件数 | 105 | 105 | 0 |
| 测试函数数 | 1457 | 1470 | **+13** |
| 测试通过数 | 1457 | 1464 | **+7** |
| 测试跳过数 | 0 | 6 | +6 |
| 源文件测试覆盖率 | 91.8%（78/85） | 91.8%（78/85） | 0 |
| `mypy: ignore-errors` 文件 | 8 | 8 | 0 |
| `mypy: disable-error-code` 文件 | 11 | 11 | 0 |
| `type: ignore` 抑制数 | 28 | 28 | 0 |
| `Any` 标注数 | 89 | 91 | **+2**（新代码引入） |
| `except Exception:` 捕获 | 7 | 7 | 0 |
| `print()` 在 `src/` | 1 | 1 | 0 |
| `pragma: no cover` | 13 | 13 | 0 |
| TODO/FIXME/HACK | 1/0/0 | 1/0/0 | 0 |
| >500 行大文件 | 18 | 20 | **+2**（pretrained_encoders + biperturb 新增越线） |
| 阻塞性问题 | 1（DEPLOY-01） | **0** | **-1** ✅ |
| 未实现功能（F-01/F-06） | 2 | **0** | **-2** ✅ |

### 5.2 模块完成度

| 模块 | 完成度 | 测试覆盖 | 类型安全 | 架构质量 |
|---|---|---|---|---|
| `src/data/` | 95% | 优良 | 中（2 文件 ignore-errors + 2 文件 disable） | 优良（大文件待拆分） |
| `src/models/` | 92% | 优良 | 差（5 文件 ignore-errors + 4 文件 disable） | 优良（davf.py 985 行居首，pretrained_encoders.py 821 行次之） |
| `src/training/` | 95% | 优良 | 良（1 文件 disable） | 优良 |
| `src/evaluation/` | 95% | 优良 | 良 | 优良 |
| `src/utils/` | 85% | 良（2 未覆盖） | 良 | 良（死代码待清理） |
| `src/api/` | 95% | 良 | 中（2 文件 disable） | 良（app.py 857 行） |
| `src/analysis/` | 90% | 良 | 中（variant_workflow.py ignore-errors） | 良 |
| `src/integration/` | 90% | 优良 | 中（genki_adapter.py 21 处 Any） | 良（ptm_virtual_perturbation.py 管线已完成） |

---

## 6. 风险与边界条件

### 6.1 已知风险

| 风险 | 可能性 | 影响 | 缓解措施 |
|---|---|---|---|
| 8 个 `ignore-errors` 文件隐藏类型错误 | 中 | 中 | 阶段 1 逐步迁移到 `disable-error-code` |
| Sphinx 文档无法构建（`index.rst` 缺失 + sphinx 未安装） | **必然** | 中 | 创建 `docs/index.rst` + 安装 sphinx |
| 不可重现构建（无锁定文件） | 中 | 高 | 生成 `requirements-lock.txt` |
| 供应链攻击（无 Dependabot） | 低 | 高 | 配置 Dependabot + pip-audit |
| `sequence_utils.py` 死代码误导维护者 | 中 | 低 | 移除或集成到调用方 |
| `genki_adapter.py` 21 处 Any 增加重构难度 | 中 | 中 | 引入 Protocol 或类型桩 |
| `external_tools.py:164` 静默吞噬异常 | 低 | 中 | 添加 logger.warning |

### 6.2 需要用户确认

- **死代码 `sequence_utils.py`**：是需要集成到 `long_sequence.py` 还是直接移除？
- **F-05 `launch_gui()`**：Streamlit GUI 是否需要继续保留开发？
- **F-02 流式处理**：是否有近期使用质谱流式数据的计划？

---

## 7. 置信度

**高** — 基于 CodeGraph 结构索引（257 文件，5326 节点，6508 边）+ 全量测试 1470 项验证（1464 通过）+ mypy 静态分析 + docker 构建检查。

**不确定性来源**：
- `except Exception:` 7 处捕获中的实际运行时影响未全部模拟
- `sequence_utils.py` 是否应集成或删除需业务决策
- sphinx 未安装，`docs/conf.py` 的完整正确性未验证

---

## 8. 附录：引用索引

| 编号 | 文件 | 行号 | 内容 |
|---|---|---|---|
| [TC-01] | `src/models/long_sequence.py` | 1 | `# mypy: ignore-errors`（985 行，最大未检查文件） |
| [TC-02] | `src/models/encoders.py` | 1 | `# mypy: ignore-errors`（762 行，中央编码器） |
| [TC-03] | `src/data/augmentation.py` | 6 | `# mypy: disable-error-code="arg-type,assignment,..."` |
| [TC-04] | `src/training/callbacks.py` | 多处 | 9 处 `# type: ignore[override]` |
| [TC-05] | `src/integration/genki_adapter.py` | 多处 | 21 处 `: Any` 标注（↑4 自 v3） |
| [MYPY-01] | mypy 输出 | — | 30 errors in 7 files |
| [ARCH-01] | `src/models/davf.py` | 985 行 | 第一大文件，需拆分 |
| [ARCH-02] | `src/models/pretrained_encoders.py` | 821 行 | 第二大文件（+296 行自 v3，因 ESM3Encoder 新增） |
| [DEAD-01] | `src/utils/sequence_utils.py` | 219 行 | 死代码，全项目无人导入 |
| [EXC-01] | `src/models/external_tools.py` | 164 | `except Exception: pass` 静默吞噬 |
| [MAGIC-01] | `src/models/davf.py` | 多处 | 硬编码 256/512/128/50 |
| [COV-01] | `src/utils/io.py` | 23 | `pragma: no cover` |
| [F-02] | `src/models/roadmap.py` | 175 | 质谱流式数据流式处理未实现 |
| [F-08] | `scripts/train_binary.py` | 47 | 硬编码 Quiescent/Activated 标签 |
| [DEP-01] | `requirements-*.txt` | 全文件 | 无锁定文件 |
| [DEP-03] | `requirements-core.txt` + `requirements-pretrained.txt` | — | `torchmetrics` 版本下限不同 |
| [DOC-01] | `docs/` | — | `index.rst` 缺失，sphinx 未安装 |

---

## 9. 与前序报告对比总结

| 维度 | v1（06-30） | v2（07-03） | v3（07-04） | v4（07-04） | 趋势 |
|---|---|---|---|---|---|
| 测试函数 | ~1100 | 1391 | 1457 | **1470** | ↑ 持续增长 |
| 测试文件 | ~90 | 101 | 105 | **105** | → 稳定 |
| `ignore-errors` | 18 | 10 | 8 | **8** | → 未改善 |
| `Any` 标注 | ~180 | 129 | 89 | **91** | → 略增（新代码引入） |
| `pragma: no cover` | ~25 | 19 | 13 | **13** | → 稳定 |
| 阻塞性问题 | 7 | 5（SEC） | 1（Docker） | **0** | ↓↓ 全部解决 ✅ |
| 死代码 | — | — | 1 文件 | **1 文件** | → 未变动 |
| 未实现功能项 | — | — | 12（F-01~F-15） | **10**（F-02~F-15 减 F-01/F-06） | ↓ 减少 2 项 ✅ |

*报告生成时间：2026-07-04*
*工具链：CodeGraph（257 files, 5326 nodes, 6508 edges）+ 全量测试 1470 项（1464 通过）+ mypy 静态分析 + 手动审查*
