# PTM2CellNet 深度代码现状复核报告

> **生成日期**: 2026-06-28
> **复核方法**: 静态代码分析 + 需求文档(AGENTS.md)交叉比对 + 多维度技术债审计
> **覆盖范围**: 全部 `src/` (48个源文件)、`tests/` (49+测试文件)、`scripts/` (23个脚本)、`configs/` (22个配置)

---

## 目录

1. [未实现的功能项](#1-未实现的功能项)
2. [未完全实现的功能](#2-未完全实现的功能)
3. [技术债分类清单](#3-技术债分类清单)
4. [风险评估矩阵](#4-风险评估矩阵)
5. [优先级修复建议](#5-优先级修复建议)

---

## 1. 未实现的功能项

以下功能在 AGENTS.md 需求文档中已定义，但代码库中完全未实现或仅存在空占位。

### 1.1 数据层未实现

| # | 需求项 | 需求来源 (AGENTS.md) | 实际状态 | 关键缺失 |
|---|--------|---------------------|---------|---------|
| 1.1.1 | `DataLoader.load_from_uniprot(accession_ids)` HTTP API 集成 | `loaders.py` 关键接口 | **未实现** | `src/data/loaders.py` 中有 `load_from_dataframe` / `load_from_csv` / `load_from_fasta` / `load_combined_data` / `load_sample_data`，但**没有任何 UniProt REST API 调用**。下载 UniProt 数据的脚本 (`scripts/download_uniprot_all_human.py`) 是独立脚本，未集成到 `DataLoader` 类 |
| 1.1.2 | 公共数据库自动下载 (PhosphoSitePlus, dbPTM, CPLM) | AGENTS.md §外部工具/API集成点 表1 | **未实现** | 没有任何 PhosphoSitePlus / dbPTM / CPLM 的 Python 自动下载器。数据必须手动下载后放入 `data/raw/` |
| 1.1.3 | 特征选择与降维 | AGENTS.md §1.3 features.py "特征选择和降维" | **未实现** | `src/data/features.py` 中实现了 `extract_onehot_sequence`、`extract_kmer_features`、`extract_physicochemical_features`、`extract_ptm_features`，但**没有 PCA/UMAP/t-SNE 等降维方法** |
| 1.1.4 | 在线数据增强 (Dataset 层) | AGENTS.md §1.4 datasets.py "在线数据增强" | **未实现** | `src/data/augmentation.py` 实现了 `SequenceAugmenter` / `PTMAugmenter`，但 `PTMDataset.__getitem__` 中**未集成在线增强**——增强仅在预处理阶段离线使用 |
| 1.1.5 | 内存优化策略 | AGENTS.md §1.4 datasets.py "内存优化" | **未实现** | `PTMDataset` 直接将整个 DataFrame 保持在内存中，没有分块加载、内存映射或惰性加载策略 |

### 1.2 模型层未实现

| # | 需求项 | 需求来源 (AGENTS.md) | 实际状态 | 关键缺失 |
|---|--------|---------------------|---------|---------|
| 1.2.1 | GRU 编码器 | AGENTS.md §2.1 "LSTM/GRU编码器" | **未实现** | `src/models/encoders.py` 中有 `CNNEncoder`、`TransformerEncoder`、`LSTMEncoder`，**但 `GRUEncoder` 不存在** |
| 1.2.2 | PTM2CellNet-Large (大规模版本) | AGENTS.md §2.4 "PTM2CellNet-Large" | **未实现** | `src/models/architectures.py` 中只有 `PTM2CellNet` 单一架构，没有独立的 Large 变体类 |
| 1.2.3 | PTM2CellNet-Ensemble (集成版本) | AGENTS.md §2.4 "PTM2CellNet-Ensemble" | **未实现** | 没有任何集成模型类。`scripts/ensemble_predict.py` 实现了预测集成的功能脚本，但没有封装的集成模型类 |
| 1.2.4 | AlphaFold 结构预测集成 | AGENTS.md §外部工具/API集成点 表2 | **未实现** | 代码库中没有 AlphaFold 相关的任何代码或配置 |
| 1.2.5 | BLAST 序列比对集成 | AGENTS.md §外部工具/API集成点 表3 | **未实现** | 代码库中没有 BLAST 相关的任何代码 |
| 1.2.6 | ClustalW 多序列比对集成 | AGENTS.md §外部工具/API集成点 表3 | **未实现** | 未实现 |
| 1.2.7 | PSIPRED 二级结构预测集成 | AGENTS.md §外部工具/API集成点 表3 | **未实现** | 未实现 |

### 1.3 训练层未实现

| # | 需求项 | 需求来源 (AGENTS.md) | 实际状态 | 关键缺失 |
|---|--------|---------------------|---------|---------|
| 1.3.1 | SGD 优化器 | AGENTS.md §3.3 "SGD优化器" | **未实现** | `src/training/optimizers.py` 中的 `configure_optimizer` 支持 AdamW / Adam / Lion，但**不支持 `torch.optim.SGD`** |
| 1.3.2 | 学习率监控回调 | AGENTS.md §3.4 "学习率监控" | **未实现** | 自定义 `Trainer` (非 Lightning) 没有任何 LR 监控回调。Lightning 版本通过 `LearningRateMonitor` 实现 |
| 1.3.3 | 进度条回调 | AGENTS.md §3.4 "进度条" | **未实现** | 自定义 `Trainer` 在训练循环中没有任何 tqdm 或进度条输出 |

### 1.4 评估层未实现

| # | 需求项 | 需求来源 (AGENTS.md) | 实际状态 | 关键缺失 |
|---|--------|---------------------|---------|---------|
| 1.4.1 | NDCG 排序指标 | AGENTS.md §4.1 "NDCG, MAP" | **未实现** | `src/evaluation/metrics.py` 中有回归指标(MAE/MSE/RMSE/R²)和分类指标(Acc/Prec/Recall/F1/AUC/MCC)，**但没有 NDCG、MAP 等排序指标** |
| 1.4.2 | `Evaluator.cross_validate()` 交叉验证 | AGENTS.md §4.2 "交叉验证评估" | **未实现** | `src/evaluation/evaluators.py` 中的 `Evaluator` 类有 `evaluate()` 和 `predict()` 方法，**但 `cross_validate()` 不存在** |
| 1.4.3 | 置信区间计算 | AGENTS.md §4.2 "置信区间计算" | **未实现** | 没有任何置信区间或 Bootstrap 不确定性估计代码 |
| 1.4.4 | 特征重要性可视化 | AGENTS.md §4.3 "特征重要性" | **未实现** | `src/evaluation/visualization.py` 实现了 ROC/PR/混淆矩阵/训练曲线/注意力热图，**但 `plot_feature_importance()` 不存在** |

### 1.5 工具层未实现

| # | 需求项 | 需求来源 (AGENTS.md) | 实际状态 | 关键缺失 |
|---|--------|---------------------|---------|---------|
| 1.5.1 | `io.load_model()` 模型加载接口 | AGENTS.md §5.3 "模型保存和加载" | **部分未实现** | `src/utils/io.py` 实现了 `save_model`，但 `load_model` 只是简单的 `torch.load` 包装，未集成配置/权重一致性验证 |
| 1.5.2 | HDF5 文件支持 | AGENTS.md §5.3 "HDF5" | **未实现** | `src/utils/io.py` 支持 pickle / JSON / CSV / NumPy，**但没有任何 HDF5 读写函数** |

### 1.6 分析层未实现

| # | 需求项 | 需求来源 (AGENTS.md) | 实际状态 | 关键缺失 |
|---|--------|---------------------|---------|---------|
| 1.6.1 | Jupyter notebooks (探索性分析) | AGENTS.md 目录结构 | **空目录** | `notebooks/exploratory/` 为空 |
| 1.6.2 | Jupyter notebooks (实验记录) | AGENTS.md 目录结构 | **空目录** | `notebooks/experiments/` 为空 |
| 1.6.3 | `configs/model/` 模型配置子目录 | AGENTS.md 目录结构 | **空目录** | `configs/model/` 存在但没有任何文件 |

### 1.7 集成层未实现

| # | 需求项 | 需求来源 | 实际状态 | 关键缺失 |
|---|--------|---------|---------|---------|
| 1.7.1 | GenKI 外部包依赖的完整集成 | AGENTS.md 未明确提及但代码中有接口 | **未完成** | `src/integration/genki/perturbation.py` 依赖外部 `GenKI.dataLoader` 包 (`importlib.import_module("GenKI.dataLoader")`)，该包不在 `requirements.txt` 中且不在代码库中 |
| 1.7.2 | `extended_pathway_kb.py` 数据资产集成 | 代码库中存在但未使用 | **完全孤立** | `src/data/extended_pathway_kb.py` (557行，6个结构化数据字典) **从未被任何代码导入**。`SignalingNetworkMapper` 和 `PathwayDatabaseIntegration` 都有自己独立的通路定义副本 |
| 1.7.3 | `src/models/signaling_network.py` 中的 `_load_pathway_db` | 代码自身 | **显式未实现** | 第142-146行显式抛出 `NotImplementedError` |

---

## 2. 未完全实现的功能

以下功能已有部分代码实现，但存在缺失组件、完成度不足或不符合需求规范的问题。

### 2.1 数据处理模块

| # | 功能 | 文件 | 完成度 | 缺失组件 |
|---|------|------|-------|---------|
| 2.1.1 | `DataPreprocessor.split_dataset` 同源性拆分 | `src/data/preprocess.py:248-255` | **~60%** | 方法委托给 `HomologyAwareSplitter`，但同源性检测逻辑 (`split_dataset` 中的 L248-255) 与 `HomologyAwareSplitter` 的职责边界不清晰 |
| 2.1.2 | `non_standard_aa_map` 标准化映射 | `src/data/preprocess.py:40-48` | **~70%** | 同一映射在 `src/data/datasets.py:58-60` 和 `ESMTokenizedDataset` (L295-297) 中重复定义了三次，未提取到共享常量 |
| 2.1.3 | 数据集统计信息 | `src/data/dataset_base.py:143-230` | **~85%** | `get_statistics` 和 `print_statistics` 混在 `PTMDatasetBase` 中，打印逻辑应分离 |

### 2.2 模型模块

| # | 功能 | 文件 | 完成度 | 缺失组件 |
|---|------|------|-------|---------|
| 2.2.1 | `PTM2CellNet.__init__` 架构组合 | `src/models/architectures.py:45-222` | **~80%** | 8种编码器的 if/elif 硬编码选择逻辑（架构复杂度 ~20），未使用工厂/注册模式。新增编码器需修改核心类 |
| 2.2.2 | `PTM2CellNet.from_config` 配置驱动初始化 | `src/models/architectures.py:191-196` | **~75%** | DAVF 相关参数硬编码默认值 (`latent_dim=10`, `num_genes=5000`, `num_steps=50`)，不从配置合并 |
| 2.2.3 | 预训练编码器 (`ESM2Encoder.encode_sequences`) | `src/models/pretrained_encoders.py:337-429` | **~85%** | 一个方法同时处理 tokenization + 滑动窗口长序列 + padding，违反单一职责原则 |
| 2.2.4 | `PerturbationGNN` (BiPerturb) | `src/models/biperturb.py:333-392` | **~60%** | 虽名为"GNN"，但仅由 `nn.Linear` 层组成，**没有消息传递或图结构操作**，是 per-gene MLP 而非真正的图神经网络 |
| 2.2.5 | `magnitudes` 参数贯穿 DAVF 栈 | 整个 DAVF 套件 | **0% (死参数)** | `magnitudes` 参数从顶层 API 到底层编码器全程传递，但在每一层都被显式标注为"保留以兼容 API，但已忽略"。这是贯穿6个文件、数百行代码的**死参数泄漏** |
| 2.2.6 | `DAVF` 与 `LatentDAVF` 代码重复 | `src/models/davf.py`, `src/models/latent_davf.py` | **~40% 共享** | 两个类共享约60%的代码（配置验证、条件解析、基因嵌入查找、清洗对齐），应提取公共基类或 mixin |

### 2.3 训练模块

| # | 功能 | 文件 | 完成度 | 缺失组件 |
|---|------|------|-------|---------|
| 2.3.1 | 自定义 `Trainer` | `src/training/trainers.py` | **~65%** | 缺少梯度裁剪、梯度累积、混合精度(AMP)、分布式训练支持、进度条、种子设置。检查点仅保存 `model.state_dict()`，缺少优化器状态/epoch/scheduler 状态，不支持恢复训练 |
| 2.3.2 | `ReduceLROnPlateau` 调度器 | `src/training/trainers.py:294` | **~50% (有bug)** | `val_logs.get("loss", default)` 的 key 是 `"loss"`，但实际 logs dict 中 key 为 `"val_loss"` (L268)，会静默传递 `None` 到 `step()` |
| 2.3.3 | `compute_class_weights` | `src/training/lightning_module.py:181` | **死代码** | 方法完整实现且通过测试，但**从未在训练管线的任何地方被调用** |
| 2.3.4 | `_apply_lora` 静默失败 | `src/training/lightning_module.py` | **~90%** | 如果 `self.model` 缺少 `encoder` 属性，仅记录警告后**继续训练但不应用 LoRA**，用户无法察觉 |
| 2.3.5 | `scripts/train.py` 训练脚本 | `scripts/train.py` | **~60%** | 无种子设置、无 `--resume` 标志、无错误处理 try/except、`num_workers=0` 硬编码 |

### 2.4 评估模块

| # | 功能 | 文件 | 完成度 | 缺失组件 |
|---|------|------|-------|---------|
| 2.4.1 | `Evaluator` 批量评估 | `src/evaluation/evaluators.py:76-131` | **~85%** | 正确处理批次但聚合方式不最优（多次 CPU-GPU 同步） |
| 2.4.2 | `CrossModalAttention` | `src/models/davf_attention.py:246-290` | **死代码** | 已完整实现但**从未在任何地方被实例化**，注释说明是"未来扩展" |

### 2.5 集成/分析模块

| # | 功能 | 文件 | 完成度 | 缺失组件 |
|---|------|------|-------|---------|
| 2.5.1 | 通路数据库集成 (`PathwayDatabaseIntegration`) | `src/analysis/pathway_integration.py` | **~70%** | 依赖 `sspa` (需要 `rpy2`，未在 requirements.txt 中列出)。缓存路径硬编码为 `./pathway_cache`。通路图是完全连接的全零权重，生物学上无意义 |
| 2.5.2 | 变异效应工作流 (`VariantEffectWorkflow`) | `src/analysis/variant_workflow.py` | **~75%** | 通路影响分析是纯存根；参考序列验证失败时静默继续；无网络重试/指数退避；离线完全无法运行 |
| 2.5.3 | GenKI 适配器 (`GenKIAdapter`) | `src/integration/genki_adapter.py` | **~80%** | `__getattr__` 动态委托使 AttributeError 无法区分"组件缺失"和"实际错误"。`validate_runtime_ready` 不验证文件实际有效性 |
| 2.5.4 | `GenKI` 后端扰动执行 | `src/integration/genki/perturbation.py` | **~75%** | VGAE 评分每个预测请求训练10个epoch，无缓存/检查点。大小写敏感的基因符号比较。依赖 `torch_geometric` 未在 requirements.txt 中列出 |
| 2.5.5 | 显著性计算 (`_build_null_distribution`) | `src/integration/genki/significance.py:227-276` | **~70%** | O(null_permutations × n_genes)，对 >100 基因和 >100 置换极端缓慢，无进度指示器。无 NaN 预清洗 |

### 2.6 API 模块

| # | 功能 | 文件 | 完成度 | 缺失组件 |
|---|------|------|-------|---------|
| 2.6.1 | `/predict/batch` 批量预测端点 | `src/api/routes/predictions.py:166-250` | **~40%性能** | **逐个样本 Python for 循环处理** (`for sample in request.samples:`)，对1000个样本产生1000次独立 GPU 调用，应为真正的 Batched 张量处理 |
| 2.6.2 | `predict` 与 `batch_predict` 代码重复 | `src/api/routes/predictions.py:96-250` | **~40%代码复用** | 两个端点共享约60%的预处理/推理/响应构建代码 |
| 2.6.3 | `src/api/routes.py` 弃用文件 | `src/api/routes.py` | **弃用存根** | 路由已移至 `routes/` 子包，但此文件保留在原地，可能导致混淆 |

---

## 3. 技术债分类清单

### 3.1 代码质量技术债

#### 3.1.1 高危

| # | 问题 | 位置 | 行号 | 描述 |
|---|------|------|------|------|
| 3.1.1 | `mypy: ignore-errors` 全局禁用 | `src/models/encoders.py` | L1 | 禁用整个文件的类型检查，覆盖真实的类型错误 |
| | | `src/models/architectures.py` | L1 | 同上 |
| | | `src/models/pooling.py` | L1 | 同上 |
| | | `src/models/model_utils.py` | L1 | 同上 |
| | | `src/api/routes/predictions.py` | L1 | 同上 |
| | | `src/data/augmentation.py` | L1 | 同上（使用 `mypy: disable-error-code` 变体） |
| | | `src/models/variant_effect.py` | L6 | 同上 |
| | | `src/analysis/pathway_integration.py` | L2 | 同上 |
| 3.1.2 | 非标准氨基酸映射三重复制 | `src/data/preprocess.py:40-48` | L40-48 | 同一映射重复出现三次 |
| | | `src/data/datasets.py` | L58-60 | 第二次定义 |
| | | `src/data/datasets.py` | L295-297 | 第三次定义 |
| 3.1.3 | `_standardize_sequence` 逻辑重复 | `src/data/datasets.py` | L62-66 | `PTMDataset` 和 `ESMTokenizedDataset` (L299-303) 有相同方法 |
| 3.1.4 | `DAVF` 与 `LatentDAVF` 约60%代码重复 | `src/models/davf.py`, `src/models/latent_davf.py` | 多处 | 配置验证、条件解析、基因嵌入查找、ID 清洗等近相同 |
| 3.1.5 | `_load_checkpoint` 加载逻辑重复 | `src/models/davf_inference.py:214-231` | L214-231 | 与 `davf_checkpoint_utils.py:93-96` 有相同 `weights_only` 回退模式 |
| 3.1.6 | `ConditionalVelocityField` 与 `LatentConditionalVelocityField` 近相同 | `davf.py` vs `latent_davf.py` | 全部 | 除输入/输出维度和残差门控机制外几乎相同 |

#### 3.1.2 中危

| # | 问题 | 位置 | 行号 | 描述 |
|---|------|------|------|------|
| 3.1.7 | `PTM2CellNet.__init__` 圈复杂度 ~20 | `src/models/architectures.py` | L45-222 | 8种编码器、3种任务类型、DAVF 条件分支嵌套 |
| 3.1.8 | 8种编码器 if/elif 硬编码选择 | `src/models/architectures.py` | L74-129 | 不使用工厂/注册模式，新增编码器需修改核心类 |
| 3.1.9 | 硬编码路径 `DAVF_CONFIG_PATH` | `src/models/architectures.py` | L32 | `configs/davf_integration.yaml` 相对路径，对 CWD 脆弱 |
| 3.1.10 | 硬编码默认值（latent_dim, num_genes 等） | `src/models/architectures.py` | L191-197 | 4个参数不从配置合并 |
| 3.1.11 | 硬编码缓存路径 `./pathway_cache` | `src/analysis/pathway_integration.py` | L67 | 相对于 CWD，不可配置 |
| 3.1.12 | 硬编码默认 checkpoint 路径 | `src/models/architectures.py` | L191 | `checkpoints/latent_davf_ibd_norman/best_model.pt` |
| 3.1.13 | 日志方式不一致 | 多处 | - | `setup_logger(__name__)` 与 `logging.getLogger(__name__)` 混用 |
| 3.1.14 | 导入方式不一致 | 多处 | - | 绝对导入 (`from src.utils.logging`) 与相对导入 (`from ..utils.logging`) 混用 |
| 3.1.15 | `non_standard_aa_map` 缺失共享常量 | 3个文件 | - | 应提取到 `src/data/__init__.py` 或新建 `src/data/constants.py` |
| 3.1.16 | `filepath.replace(".pt", "_last.pt")` 脆弱替换 | `src/training/callbacks.py` | L106 | 如果路径中含有 `.pt` 子串会错误替换 |
| 3.1.17 | `assert self.head_dim * num_heads == hidden_dim` | `src/models/pooling.py` | L37 | 生产环境被 `-O` 优化掉，不执行检查 |

#### 3.1.3 低危

| # | 问题 | 位置 | 行号 | 描述 |
|---|------|------|------|------|
| 3.1.18 | Mamba 编码器 magic numbers | `src/models/mamba_encoder.py` | L30-32 | `d_state=16`, `d_conv=4`, `expand_factor=2` 无命名常量 |
| 3.1.19 | FocalLoss alpha 类型检查分支复杂 | `src/training/losses.py` | L91-164 | 圈复杂度 ~12 |
| 3.1.20 | `CrossModalAttention` 死代码 | `src/models/davf_attention.py` | L246-290 | 完整实现但从未实例化 |
| 3.1.21 | `src/api/routes.py` 弃用存根 | `src/api/routes.py` | L1-19 | 整个文件保持原样但路由已移至子包 |
| 3.1.22 | DAVF 集成注释包含陈旧阶段引用 | `src/models/architectures.py:22`, `src/models/__init__.py:36` | 多处 | `# DAVF Integration imports (Phase 11, 12)` 等 |
| 3.1.23 | `GeneSpecificModulation` 初始化脆弱 | `src/models/davf.py` | L239-241 | `film_generator[-1].bias.copy_(...)` 假设 `film_generator` 最后是 Linear 层 |

### 3.2 架构技术债

#### 3.2.1 高危

| # | 问题 | 位置 | 描述 |
|---|------|------|------|
| 3.2.1 | **模型→训练循环依赖** | `pretrained_encoders.py` 导入 `training/peft_config` | `src/models/pretrained_encoders.py:L276,302,320` 从 `src.training.peft_config` 导入 LoRA 应用/保存/加载函数，创建了 `training → models → training` 的循环依赖链 |
| 3.2.2 | **setup.py 依赖列表不完整** | `setup.py` | 只列出 requirements.txt 中 29+ 个包的10个，`pip install .` 安装一个缺失一半依赖的不完整包 |
| 3.2.3 | **configs/ 未被 git 跟踪** | `.gitignore` | `.gitignore` 的白名单模式（`/*` + `!` 豁免）未包含 `configs/`，所有22个 YAML 配置文件不在版本控制中 |
| 3.2.4 | **Docker GPU 配置不可用** | `Dockerfile`, `docker-compose.yml` | Dockerfile 基于 `python:3.10-slim`（无 CUDA），但 `docker-compose.yml` 训练服务请求 GPU 资源，GPU 完全不工作 |

#### 3.2.2 中危

| # | 问题 | 位置 | 描述 |
|---|------|------|------|
| 3.2.5 | 配置键结构不一致 | `configs/` 全部 | `training` 键在部分配置中是扁平结构，在部分配置中是嵌套结构。`num_classes` 在部分配置中缺失。PTM 类型名大小写不一致 (`"Phosphorylation"` vs `"phosphorylation"`) |
| 3.2.6 | 两个 `PTMDataModule` 类重复 | `src/data/datasets.py:125`, `src/data/lightning_datamodule.py:20` | 独立版和 Lightning 版本在 DataLoader 配置有细微不同(`batch_size=32` 相同但 `num_workers=0` vs `num_workers=4`) |
| 3.2.7 | `Config` 类无模式验证 | `src/utils/config.py` | 接受任意 YAML 结构，`get()` 静默返回 `None`，配置错误在运行时暴露而非加载时 |
| 3.2.8 | 通路数据三重复制 | `extended_pathway_kb.py`、`signaling_network.py`、`pathway_integration.py` | 三个文件各自维护独立的通路定义副本 |
| 3.2.9 | `PerturbationGNN` 名不副实 | `src/models/biperturb.py:L333-392` | 名含"GNN"但无图操作，仅为 per-gene MLP |
| 3.2.10 | `magnitudes` 死参数贯穿6文件 | DAVF 套件全部 | 在每一层被忽略但仍强制调用者传递 |

#### 3.2.3 低危

| # | 问题 | 位置 | 描述 |
|---|------|------|------|
| 3.2.11 | 编码器选择未用注册模式 | `architectures.py` | if/elif 链扩展性差 |
| 3.2.12 | `DataValidator` 混合验证与报告 | `src/data/validation.py` | 验证与打印输出混合 |
| 3.2.13 | `_apply_lora` 静默失败 | `lightning_module.py` | 缺少 encoder 时仅 warn 不报错 |
| 3.2.14 | `no_init_weights` 临时属性协议 | `davf.py` | 跳过初始化的 ad-hoc 属性检查，重构时可能被忽略 |
| 3.2.15 | `z_0` 参数被接受但忽略 | `davf_inference.py` | 调用者提供但静默丢弃 |

### 3.3 性能技术债

#### 3.3.1 高危

| # | 问题 | 位置 | 行号 | 描述 |
|---|------|------|------|------|
| 3.3.1 | **Mamba SSM Python for 循环** | `src/models/mamba_encoder.py` | L164-174 | SSM 递推使用 `for t in range(seq_len)` 逐元素循环，对长度1000的蛋白产生1000次独立 GPU kernel 启动。官方 Mamba 使用 associative scan 或自定义 CUDA kernel |
| 3.3.2 | **批量预测端点逐个样本处理** | `src/api/routes/predictions.py` | L166-250 | `/predict/batch` 使用 Python `for sample in request.samples:` 逐个处理，对1000样本产生1000次 GPU 调用，浪费批处理并行性 |

#### 3.3.2 中危

| # | 问题 | 位置 | 行号 | 描述 |
|---|------|------|------|------|
| 3.3.3 | DataFrame 重复 `.copy()` | `src/data/preprocess.py` | L72, L108, L151 | 三个预处理方法各自调用 `df.copy()`，即使无修改 |
| 3.3.4 | `standardize_amino_acids` 多次 DataFrame 遍历 | `src/data/preprocess.py` | L75-85 | 对每个非标准字符分别 `apply(lambda s: s.count(...))`，O(N×M) 多次遍历 |
| 3.3.5 | `_get_gene_embeddings` Python 级循环 | `src/models/davf.py:L746-757`, `latent_davf.py:L443-482` | B×K 次 Python 循环，大批量时极端低效 |
| 3.3.6 | `clean_sequences` 行级迭代 | `src/data/preprocess.py` | L112-128 | `.items()` 逐行迭代，未向量化 |
| 3.3.7 | 自定义 `Trainer` 无混合精度 | `src/training/trainers.py` | - | 无 AMP，float32 全精度训练，内存翻倍、吞吐量减半 |

#### 3.3.3 低危

| # | 问题 | 位置 | 描述 |
|---|------|------|------|
| 3.3.8 | Mamba 隐状态每次前向重分配 | `mamba_encoder.py:L158` | `h = torch.zeros(...)` 每次 forward 新建，无内存池 |
| 3.3.9 | `Config.to_dict()` 每次 deep copy | `model_utils.py:L114` | `copy.deepcopy` 对大型配置昂贵 |
| 3.3.10 | Geneformer 模型加载后不释放 | `geneformer_embedding.py:L174-178` | 加载完整 Transformer 后只取 embedding 权重，`_model` 保持引用不释放 |
| 3.3.11 | `extract_features` 逐个样本调用 | `features.py:L272-276` | 内部循环应向量化 |
| 3.3.12 | `_build_null_distribution` 无进度指示器 | `significance.py:L227-276` | 极端耗时的 O(n×m) 操作无反馈 |
| 3.3.13 | 序列标准化单次 `replace()` 六遍 | `datasets.py:L62-66` | 单次翻译表可替换 |

### 3.4 安全技术债

#### 3.4.1 高危

| # | 问题 | 位置 | 行号 | 描述 |
|---|------|------|------|------|
| 3.4.1 | **`pickle.load()` 无白名单 - 任意代码执行** | `src/data/validation.py` | L226 | `DatasetCache.load()` 使用 `pickle.load(f)` 无任何白名单。攻击者写入 `.pkl` 文件到 `cache_dir` 可导致 RCE |
| 3.4.2 | **`pickle.load()` 无白名单 - 通路缓存** | `src/analysis/pathway_integration.py` | L103 | `self.kegg_pathways = pickle.load(f)` 同样问题 |
| 3.4.3 | **`pickle.load()` 无白名单 - 通路缓存** | `src/analysis/pathway_integration.py` | L151 | `self.reactome_pathways = pickle.load(f)` 同样问题 |

#### 3.4.2 中危

| # | 问题 | 位置 | 行号 | 描述 |
|---|------|------|------|------|
| 3.4.4 | `weights_only=False` 回退 | `src/models/davf_inference.py` | L227-231 | 为兼容旧检查点允许不安全加载，如果检查点来自不可信来源导致 RCE |
| 3.4.5 | `weights_only=False` 回退（重复） | `src/models/davf_checkpoint_utils.py` | L93-96 | 同上 |
| 3.4.6 | CORS 过于宽松 | `src/api/app.py` | L62-63 | `allow_methods=["*"], allow_headers=["*"]` |
| 3.4.7 | 检查点路径来自配置无校验 | `src/models/davf_inference.py` | L202 | `Path(self.config.checkpoint_path)` 配置中的路径未经路径穿越校验 |

#### 3.4.3 低危

| # | 问题 | 位置 | 描述 |
|---|------|------|------|
| 3.4.8 | `_SafeUnpickler` 白名单包含 numpy bypass 向量 | `src/utils/io.py:L82` | `numpy.core.multiarray._reconstruct` 已知有绕过可能 |
| 3.4.9 | API endpoint 中 uniprot_id 输入校验不足 | `routes/predictions.py:L277` | 仅类型检查，无格式/内容校验 |
| 3.4.10 | Docker 以 root 运行 | `Dockerfile` | 无 `USER appuser` |
| 3.4.11 | 无 `.dockerignore` | 项目根目录 | 可能将 secrets 或 `__pycache__` 包含到构建上下文中 |

### 3.5 文档技术债

#### 3.5.1 中危

| # | 问题 | 位置 | 描述 |
|---|------|------|------|
| 3.5.1 | `src/integration/genki/` 子模块缺少方法文档字符串 | `graph_utils.py`, `perturbation.py`, `significance.py` | 类有一句话摘要，但多数方法无文档字符串 |
| 3.5.2 | `src/integration/genki_adapter.py` 公共方法缺少文档字符串 | `genki_adapter.py` | 除 `get_backend_info`、`validate_runtime_ready` 外的公共方法无描述 |
| 3.5.3 | `src/integration/ptm_virtual_perturbation.py` 缺少文档字符串 | `ptm_virtual_perturbation.py` | `PTMPerturbationProfile` 和 `apply_soft_perturbation` 无描述 |
| 3.5.4 | `src/data/extended_pathway_kb.py` 完全未接入任何代码 | `extended_pathway_kb.py` | 557 行数据零引用，无导入说明 |

#### 3.5.2 低危

| # | 问题 | 位置 | 描述 |
|---|------|------|------|
| 3.5.5 | 中英文文档字符串混用 | 多处 | `extended_pathway_kb.py` 全中文注释；`signaling_network.py` 中英混合；`variant_effect.py` 全中文；其他文件全英文。无统一语言策略 |
| 3.5.6 | `get_embedding_dim()` 文档字符串 `"192 for Geneformer"` 错误 | `geneformer_embedding.py:L296-297` | 实际 Geneformer 维度为 1152 |
| 3.5.7 | `variant_workflow.py:188-196` `_resolve_variant_uniprot_id` 无文档字符串 | `variant_workflow.py` | 缺失 |
| 3.5.8 | `cross_validate` 方法不存在但文档字符串有提及 | `evaluators.py` 设计文档 | 接口契约定义了但未实现 |
| 3.5.9 | `notebooks/` 空目录无说明 | 项目根目录 | 两个 notebook 目录为空，无 README 说明用途 |

### 3.6 测试技术债

#### 3.6.1 高危

| # | 问题 | 位置 | 描述 |
|---|------|------|------|
| 3.6.1 | **GPU-only 测试 — CI 上永不被执行** | `tests/unit/test_models.py:L403-406` 等 | 6+ 处 `@pytest.mark.skipif(not torch.cuda.is_available(), ...)`。测试 `test_models.py` 中约60%的代码在 CPU 环境下静默跳过，包括所有 ESM-2/ProtBERT 编码器测试 |
| 3.6.2 | **测试只检查 `is not None` 不验证内容** | `tests/unit/test_data.py:L279-286` | `test_val_dataloader` 和 `test_test_dataloader` 只断言 `assert val_loader is not None` |
| 3.6.3 | **过度 Mock 后测试全部跳过** | `tests/unit/test_models.py:L344-466` | `TestPTM2CellNetWithPretrained` 类先 mock 全部 transformers，然后全部 GPU-skip，整个测试类在 CPU 上是死代码 |
| 3.6.4 | `test_fast_dev_run` 是空操作 | `tests/unit/training/test_lightning_module.py:L476` | 只断言 `trainer is not None` 和 `module is not None`，注释写"只验证Trainer可以初始化模块" |

#### 3.6.2 中危

| # | 问题 | 位置 | 描述 |
|---|------|------|------|
| 3.6.5 | 异常处理中空 `pass` | `tests/unit/test_train_pretrained_script.py:L69,129,139` | 测试静默吞噬异常 |
| 3.6.6 | 边缘情况缺失：空输入、NaN、极值 | 多数测试文件 | 无 `DataPreprocessor` 空 DataFrame 测试，无 `PTMDataset` NaN 序列测试，无极端长度/数量测试 |
| 3.6.7 | LoRA 测试 mock `_apply_lora` 全部 | `tests/unit/training/test_lightning_module.py:L419-453` | 三个测试全部 mock 方法本身，仅测试"配置标志触发了调用"而非"LoRA 被正确应用" |
| 3.6.8 | 检查点恢复测试不完整 | `tests/unit/training/test_trainers.py` | `test_resume_training` 只验证 `state_dict` 可保存/加载，不验证优化器状态和 epoch 连续性 |
| 3.6.9 | 回归评估指标未测试 | `tests/unit/evaluation` | `Evaluator._calculate_regression_metrics` 未被任何测试覆盖 |
| 3.6.10 | `UndefinedMetricWarning` 全局抑制 | `src/evaluation/metrics.py:L46-47` | `warnings.filterwarnings("ignore", ...)` 抑制后不恢复，影响其他测试 |

#### 3.6.3 低危

| # | 问题 | 位置 | 描述 |
|---|------|------|------|
| 3.6.11 | 随机种子未隔离 | `tests/unit/evaluation/test_metrics.py:L189-195` | `np.random.seed(42)` 可能干扰其他种子依赖的测试 |
| 3.6.12 | `NamedTemporaryFile(delete=False)` 泄露 | `tests/unit/test_data.py:L35-45` | 崩溃时临时文件残留 |
| 3.6.13 | `pytest.ini` 定义但未使用的 markers | `pytest.ini` | `gpu`, `slow`, `integration` 三个 marker 定义但没有任何测试装饰器使用 |
| 3.6.14 | `test_full_pipeline` 不验证收敛 | `tests/integration/test_integration.py` | 用3个epoch训练100样本但不验证指标是否 > 随机基线 |

### 3.7 依赖管理技术债

#### 3.7.1 高危

| # | 问题 | 位置 | 描述 |
|---|------|------|------|
| 3.7.1 | `setup.py` 与 `requirements.txt` 严重不一致 | `setup.py` | `pip install -e .` 缺少 `lightning`、`transformers`、`peft`、`fastapi`、`uvicorn`、`hgvs`、`uniprot-id-mapper`、`anndata`、`requests`、`networkx`、`einops`、`torchmetrics`、`lion-pytorch`、`torchvision` 等一半以上的依赖 |
| 3.7.2 | `torch_geometric` 缺失（使用但未列出） | `src/integration/genki/perturbation.py:L270` | VGAE 评分必需，无 fallback |
| 3.7.3 | `scanpy` 缺失（被探测但未列出） | `src/integration/genki/reference_data.py:L101` | `genki_source` 后端必需 |
| 3.7.4 | `lightning>=2.5.0` 与 `torch>=1.10.0` 冲突 | `requirements.txt` | Lightning 2.5+ 要求 PyTorch ≥2.1，但约束允许 `torch==1.10.0` |

#### 3.7.2 中危

| # | 问题 | 位置 | 描述 |
|---|------|------|------|
| 3.7.5 | `safetensors` 缺失 | `src/models/geneformer_embedding.py:L111,144` | 代码使用 `from safetensors.torch import load_file`，但 `safetensors` 不在 requirements.txt 中（作为 transformers 的传递依赖可能安装但未显式声明） |
| 3.7.6 | `rpy2` 缺失（SSPA 的传递依赖） | `src/analysis/pathway_integration.py:L11` | `sspa` 需要 `rpy2` 但未在任何地方列出 |
| 3.7.7 | `GenKI` 外部包缺失 | `src/integration/genki/perturbation.py:L247` | 通过 `importlib` 动态导入，不存在于代码库或 requirements.txt |
| 3.7.8 | `pydantic>=1.8.0` 与 FastAPI 新版本不兼容 | `requirements.txt` | FastAPI 0.100+ 需要 Pydantic v2，但约束允许安装 v1 |
| 3.7.9 | 三个包被列入但从未导入 | `requirements.txt` | `torchvision>=0.11.0`、`python-dotenv>=0.19.0`、`h5py>=3.6.0` 在代码库中零引用 |

#### 3.7.3 低危

| # | 问题 | 位置 | 描述 |
|---|------|------|------|
| 3.7.10 | 可选依赖未标记 | `requirements.txt` | `peft`、`lion-pytorch`、`torchmetrics`、`sspa` 均被条件导入但仅 `sspa` 标记为 `# optional` |
| 3.7.11 | `pytest` 不在任何依赖列表中 | `requirements.txt` / `setup.py` | 49+ 测试文件的必要依赖，应放在 `extras_require["dev"]` 或 `extras_require["test"]` |
| 3.7.12 | 无 `extras_require` 定义 | `setup.py` | 无 dev/test/gpu 等可选依赖组 |
| 3.7.13 | 无 entry_points | `setup.py` | 无 CLI 命令定义，脚本需 `python scripts/train.py` 运行 |
| 3.7.14 | `weight_decay` 百倍差异 | `configs/default.yaml` vs 其他 | 默认 `1e-4`，其他所有配置 `0.01` |
| 3.7.15 | Docker healthcheck 路径可能错误 | `Dockerfile` | `http://localhost:8000/health` — API 路由可能挂在 `/api/v1` 下，需确认是否有裸 `/health` 端点 |

---

## 4. 风险评估矩阵

### 4.1 按严重级别汇总

| 类别 | 高危 | 中危 | 低危 | 合计 |
|------|------|------|------|------|
| **未实现功能** | 3 (外部工具集成) | 8 (优化器/指标/notebook) | 16 (可视化和存根) | 27 |
| **未完全实现** | 2 (死参数/训练器缺失) | 8 (重复代码/性能bug) | 10 (文档/配置) | 20 |
| **代码质量债** | 6 (mypy屏蔽/代码重复) | 11 (圈复杂度/硬编码) | 6 (magic number/死代码) | 23 |
| **架构债** | 4 (循环依赖/setup/git/Docker) | 7 (配置/重复/验证) | 5 (注册模式/混合职责) | 16 |
| **性能债** | 2 (Mamba循环/单样本批处理) | 6 (数据拷贝/AMP缺失) | 6 (内存/池化/进度) | 14 |
| **安全债** | 3 (pickle RCE) | 4 (unsafe加载/CORS) | 3 (numpy绕过/root运行) | 10 |
| **文档债** | 0 | 4 (缺少文档字符串/孤立KB) | 5 (中英混/错误数值) | 9 |
| **测试债** | 4 (GPU-skip/空断言/过度mock) | 6 (静默pass/边缘缺失) | 4 (种子隔离/未用marker) | 14 |
| **依赖债** | 4 (setup缺失/冲突/依赖缺失) | 6 (未标记/版本不兼容) | 5 (可选标记/entrypoint) | 15 |
| **合计** | **28** | **60** | **60** | **148** |

### 4.2 影响 vs 可能性矩阵

```
影响\可能性    低              中              高
高             3.1.7-9(硬编码)  3.4.1-3(pickle)  3.2.1(循环依赖)
                                      3.3.1(Mamba)    3.7.1(setup)
                                      3.3.2(批处理)   
                                      3.6.1(GPU-skip)
中             3.1.1(mypy屏蔽)  2.5.1(通路部分)   3.2.2(setup依赖)
               3.5.2(文档缺失)  2.3.1(LR bug)     3.2.3(configs未跟踪)
               3.7.9(无用依赖)  2.6.1(批处理)      3.2.4(Docker GPU)
                                      3.4.4-5(unsafe)
低             3.1.18(magic)    3.5.1(KB孤立)     2.2.5(死参数)
               3.3.8(内存池)    3.6.2(空断言)      2.3.3(死代码)
               3.7.12(extras)   3.6.3(过度mock)   
```

---

## 5. 优先级修复建议

### P0 — 立即修复（生产阻塞）

| 优先级 | 问题# | 描述 | 建议修复 |
|--------|-------|------|---------|
| P0 | 3.4.1-3 | `pickle.load()` 无白名单 (RCE) | 替换为 `safetensors` 或 JSON 序列化，或使用 `_SafeUnpickler` 严格白名单 |
| P0 | 3.2.1 | 模型→训练循环依赖 | 将 `peft_config.py` 移至 `utils/` 或 `models/` 下的共享位置 |
| P0 | 3.7.1 | `setup.py` 依赖严重缺失 | 同步 `setup.py` 的 `install_requires` 与 `requirements.txt` |
| P0 | 2.3.1 | 自定义 `Trainer` 无梯度裁剪/AMP | 添加 `torch.nn.utils.clip_grad_norm_()` 和 `torch.cuda.amp` 支持 |

### P1 — 高优先级（阻碍开发或部署）

| 优先级 | 问题# | 描述 | 建议修复 |
|--------|-------|------|---------|
| P1 | 3.2.3 | `configs/` 未跟踪 | 在 `.gitignore` 中添加 `!configs/` 豁免 |
| P1 | 3.2.4 | Docker GPU 配置不可用 | 基于 `nvidia/cuda:12.1.0-runtime-ubuntu22.04` 构建 |
| P1 | 3.3.2 | 批量预测逐个样本处理 | 改为真正 batched 张量处理 |
| P1 | 3.6.1 | GPU-only 测试在 CI 静默跳过 | 添加 CPU 兼容的回退路径或条件测试 |
| P1 | 3.3.1 | Mamba SSM Python 循环 | 使用 `torch.associative_scan` 替代 for 循环 |
| P1 | 3.7.4 | `lightning>=2.5.0` 与 `torch>=1.10.0` 冲突 | 设置 `torch>=2.1.0` |
| P1 | 1.4.2 | `Evaluator.cross_validate()` 缺失 | 实现基本的 K-fold 交叉验证 |
| P1 | 3.1.1 | 8个源文件 `mypy: ignore-errors` | 逐个修复类型错误后移除全局忽略 |

### P2 — 中优先级（提升质量和可维护性）

| 优先级 | 问题# | 描述 | 建议修复 |
|--------|-------|------|---------|
| P2 | 3.1.2-3 | 非标准氨基酸映射三重复制 | 提取到 `src/data/constants.py` |
| P2 | 3.1.4 | `DAVF` 与 `LatentDAVF` 60%代码重复 | 提取公共基类 |
| P2 | 2.2.5 | `magnitudes` 死参数 | 移除贯穿6文件的死参数 |
| P2 | 3.2.5 | 配置键结构不一致 | 统一配置 schema，添加 Pydantic 验证 |
| P2 | 2.3.2 | `ReduceLROnPlateau` key 不匹配 bug | 修复 `val_loss` vs `loss` key 问题 |
| P2 | 3.6.4 | `test_fast_dev_run` 空操作 | 实现真正的快速运行测试 |
| P2 | 3.4.6 | CORS 过于宽松 | 限制 `allow_methods` 和 `allow_headers` |
| P2 | 3.7.2-3 | 缺失的 `torch_geometric`/`scanpy` | 添加到 requirements.txt，添加 fallback 和清晰错误 |
| P2 | 3.5.1-3 | 文档字符串缺失 | 至少为公共 API 添加文档字符串 |

### P3 — 低优先级（整洁和改进）

| 优先级 | 问题# | 描述 |
|--------|-------|------|
| P3 | 3.1.7-8 | 编码器 if/elif 链改为工厂模式 |
| P3 | 1.2.1 | 实现 GRU 编码器 |
| P3 | 1.3.1 | 实现 SGD 优化器支持 |
| P3 | 3.3.3-6 | DataFrame 操作向量化优化 |
| P3 | 2.6.1-2 | `/predict` 与 `/batch_predict` 代码去重 |
| P3 | 3.5.5 | 中英文文档字符串统一 |
| P3 | 3.7.9 | 移除未使用的 `torchvision`/`python-dotenv`/`h5py` |
| P3 | 1.6.1-2 | 添加示例 notebooks 或明确说明文档 |
| P3 | 3.2.9 | `PerturbationGNN` 重命名或实现图操作 |
| P3 | 2.4.2 | `CrossModalAttention` 接入或移除 |
| P3 | 3.7.12-13 | `setup.py` 添加 `extras_require` 和 `entry_points` |

---

## 附录
