# PTM2CellNet 深度代码现状复核报告 v2

> **生成日期**: 2026-06-28
> **复核方法**: ODW多Agent并行静态代码分析 + 6个并行Agent深度调研 + 逐文件源代码验证
> **覆盖范围**: 全部 `src/` (80+源文件)、`tests/` (49+测试文件)、`scripts/` (22个脚本)、`configs/` (22个配置)、`Dockerfile`、`docker-compose.yml`、`setup.py`、`requirements.txt`
> **基于报告**: `深度代码现状复核报告.md` (v1) 的勘误与补充

---

## 目录

1. [v1 报告勘误表](#1-v1-报告勘误表)
2. [未实现的功能项](#2-未实现的功能项)
3. [未完全实现的功能](#3-未完全实现的功能)
4. [新发现的问题（v1 未覆盖）](#4-新发现的问题v1-未覆盖)
5. [技术债分类清单](#5-技术债分类清单)
6. [风险评估矩阵](#6-风险评估矩阵)
7. [优先级修复建议](#7-优先级修复建议)

---

## 1. v1 报告勘误表

以下条目在 v1 报告中标记为"未实现"或"存在问题"，但实际代码已实现或设计正确。

| # | v1 报告条目 | v1 状态 | 实际状态 | 关键证据 |
|---|-----------|---------|---------|---------|
| 1.1 | GRU 编码器 (`1.2.1`) | **未实现** — 报告称 `GRUEncoder` 不存在 | ✅ **已实现** | `src/models/encoders.py:145-175` — 完整的 `GRUEncoder` 类，含 `__init__`、`forward`；在 `architectures.py:14` 导入，`__init__.py:8` 导出 |
| 1.2 | PTM2CellNet-Large (`1.2.2`) | **未实现** — 报告称无独立 Large 变体 | ✅ **已实现** | `src/models/architectures.py:29-404` — `PTM2CellNetLarge` 完整类（`__init__`、`forward`、`get_model_info`、`from_config`），默认 `embed_dim=256, num_layers=4, num_heads=8` |
| 1.3 | PTM2CellNet-Ensemble (`1.2.3`) | **未实现** — 报告称无封装模型类 | ✅ **已实现** | `src/models/ensemble.py:18-153` — `PTM2CellNetEnsemble`，支持 `mean` / `voting` / `weighted` 聚合策略 |
| 1.4 | `Evaluator.cross_validate()` (`1.4.2`) | **未实现** — 报告称方法不存在 | ✅ **已实现** | `src/evaluation/evaluators.py:170-271` — 完整 K-fold 交叉验证实现，含索引分割、各 fold 评估、均值/标准差汇总 |
| 1.5 | NDCG/MAP 排序指标 (`1.4.1`) | **未实现** — 报告称不存在 | ✅ **已实现** | `src/evaluation/metrics.py:286-343` (`calculate_ndcg`)、`:346-387` (`calculate_map`)、`:390-409` (`calculate_ranking_metrics`) |
| 1.6 | `plot_feature_importance` (`1.4.4`) | **未实现** — 报告称不存在 | ✅ **已实现** | `src/evaluation/visualization.py:220-253` — 水平条形图，含 `top_n=20` 参数和 `_ensure_parent_dir` |
| 1.7 | SGD 优化器 (`1.3.1`) | **未实现** — 报告称不支持 | ✅ **已实现** | `src/training/optimizers.py:102-108` — `elif optimizer_type == "sgd"` 分支，含 `momentum=0.9` |
| 1.8 | ReduceLROnPlateau `val_loss` key bug (`2.3.2`) | **存在 bug** — 报告称 key 不匹配 | ❌ **非 bug** | `_val_epoch()` 返回 `{"loss": avg_loss, "accuracy": ...}` (L221)，所以 `val_logs.get("loss", ...)` 的 key `"loss"` 是正确的。`val_logs` 字典和 `logs` 字典（使用 `val_` 前缀）是不同消费者，各自有正确的 key |
| 1.9 | 进度条回调 (`1.3.3`) | **未实现** — 报告称自定义 Trainer 无进度条 | ⚠️ **可通过回调获得** | `src/training/callbacks.py:275-375` — `ProgressBarCallback` 完整实现，可通过 `Trainer.add_callback()` 使用 |
| 1.10 | 学习率监控回调 (`1.3.2`) | **未实现** | ⚠️ **可通过回调获得** | `src/training/callbacks.py:210-264` — `LearningRateMonitor` 完整实现，可通过回调添加 |

**勘误结论**: v1 报告中有 **8 条"未实现"标记是错误**的（上述 1.1-1.7 加上 1.8 的误判），另有 2 条（1.9-1.10）描述不够准确。这些错误可能源于 AI 生成报告的幻觉或对需求文档的机械比对。

---

## 2. 未实现的功能项

以下功能在 AGENTS.md 或交付物清单中定义，但代码中完全未实现。

### 2.1 数据层未实现

| # | 需求项 | 需求来源 | 实际状态 | 关键缺失 |
|---|--------|---------|---------|---------|
| 2.1.1 | `DataLoader.load_from_uniprot(accession_ids)` | AGENTS.md `loaders.py` 关键接口 | **未实现** | `src/data/loaders.py` 有 `load_from_csv` / `load_from_fasta` / `load_from_json` / `load_sample_data` / `load_combined_data`，但**无 UniProt REST API 调用** |
| 2.1.2 | 公共数据库自动下载 (PhosphoSitePlus, dbPTM, CPLM) | AGENTS.md §外部工具/API集成点 表1 | **未实现** | 无 Python 自动下载器。数据必须手动下载后放入 `data/raw/` |
| 2.1.3 | 特征选择与降维 (PCA/UMAP/t-SNE) | AGENTS.md §1.3 features.py | **未实现** | `src/data/features.py` 无 PCA/UMAP/t-SNE 等方法。仅有一对 `extract_features_for_sample` 和 `extract_features` 批处理方法 |
| 2.1.4 | 在线数据增强集成 | AGENTS.md §1.4 datasets.py | **未实现** | `src/data/augmentation.py` 实现了 `SequenceAugmenter` / `PTMAugmenter`，但 `PTMDataset.__getitem__` (datasets.py:103) 中**未调用任何增强器** |
| 2.1.5 | 内存优化策略 | AGENTS.md §1.4 datasets.py | **未实现** | `PTMDataset` 直接将整个 DataFrame 保持在内存中，无分块加载、内存映射或惰性加载 |

### 2.2 模型层未实现

| # | 需求项 | 需求来源 | 实际状态 | 关键缺失 |
|---|--------|---------|---------|---------|
| 2.2.1 | AlphaFold 结构预测集成 | AGENTS.md §外部工具/API集成点 表2 | **仅存根** | `src/models/external_tools.py:16-57` — `AlphaFoldClient` 所有方法抛出 `NotImplementedError`，`check_available()` 返回 `False` |
| 2.2.2 | BLAST 序列比对集成 | AGENTS.md §外部工具/API集成点 表3 | **仅存根** | `src/models/external_tools.py:59-100` — `BLASTClient` 同样全存根 |
| 2.2.3 | ClustalW 多序列比对集成 | AGENTS.md §外部工具/API集成点 表3 | **仅存根** | `src/models/external_tools.py:102-142` — `ClustalWClient` 存根 |
| 2.2.4 | PSIPRED 二级结构预测集成 | AGENTS.md §外部工具/API集成点 表3 | **仅存根** | `src/models/external_tools.py:144-183` — `PSIPREDClient` 存根 |

### 2.3 训练层未实现

| # | 需求项 | 需求来源 | 实际状态 | 关键缺失 |
|---|--------|---------|---------|---------|
| 2.3.1 | 自定义 Trainer 梯度裁剪 | AGENTS.md §3.1 训练循环 | **未实现** | `src/training/trainers.py:107-160` — `_train_epoch` 中 `loss.backward()` 后无 `clip_grad_norm_`/`clip_grad_value_` |
| 2.3.2 | 自定义 Trainer 混合精度 (AMP) | AGENTS.md §3.1 | **未实现** | 无 `torch.cuda.amp.autocast` 或 `GradScaler` |
| 2.3.3 | 自定义 Trainer 分布式训练 | AGENTS.md §3.1 | **未实现** | 无 DataParallel/DDP 支持 |
| 2.3.4 | 自定义 Trainer 训练恢复 | AGENTS.md §3.1 检查点 | **未实现** | 检查点仅保存 `model.state_dict()`，缺少优化器状态/epoch/scheduler 状态 |

### 2.4 评估层未实现

| # | 需求项 | 需求来源 | 实际状态 | 关键缺失 |
|---|--------|---------|---------|---------|
| 2.4.1 | 置信区间计算 | AGENTS.md §4.2 | **未实现** | 无 Bootstrap 或其它不确定性估计代码 |
| 2.4.2 | `PTMDataModule.predict_dataloader()` | Lightning 规范 | **未实现** | `src/data/lightning_datamodule.py` 缺少 `predict_dataloader()` 方法 |

### 2.5 工具/基础设施层未实现

| # | 需求项 | 需求来源 | 实际状态 | 关键缺失 |
|---|--------|---------|---------|---------|
| 2.5.1 | Jupyter notebooks (探索性分析) | AGENTS.md 目录结构 | **空目录** | `notebooks/exploratory/` 为空 |
| 2.5.2 | Jupyter notebooks (实验记录) | AGENTS.md 目录结构 | **空目录** | `notebooks/experiments/` 为空 |
| 2.5.3 | `configs/model/` 模型配置子目录 | AGENTS.md 目录结构 | **空目录** | `configs/model/` 存在但无文件 |
| 2.5.4 | 训练脚本种子设置 | 可重复性 | **19/22 脚本缺失** | 仅 `train_multitask_ptm.py`、`homology_split.py`、`validate_cptac.py` 设置了随机种子 |
| 2.5.5 | 训练脚本 `--resume` 支持 | 训练流程 | **19/22 脚本缺失** | 仅 `train_ptm_site.py`、`train_lightning.py`、`train_pretrained.py` 支持 |
| 2.5.6 | 训练/预测/评估脚本顶层 try/except | 生产健壮性 | **14/22 脚本缺失** | 核心脚本 `train.py`、`predict.py`、`evaluate.py`、`train_binary.py`、`train_lightning.py`、`train_pretrained.py` 等在异常时直接崩溃 |

---

## 3. 未完全实现的功能

| # | 功能 | 文件 | 完成度 | 缺失组件 |
|---|------|------|-------|---------|
| 3.1 | `PTM2CellNet.__init__` 编码器选择（`architectures.py:78-140`） | `architectures.py` | **~80%** | 8种编码器的 if/elif 硬编码选择，未使用工厂/注册模式。新增编码器需修改核心类 |
| 3.2 | 自定义 `Trainer`（`trainers.py:27-344`） | `trainers.py` | **~65%** | 缺少梯度裁剪、AMP、分布式训练、种子设置、--resume 支持。检查点仅存 `state_dict` |
| 3.3 | `DAVF` 与 `LatentDAVF` 共享抽象 | `davf.py` + `latent_davf.py` | **~40% 共享** | 约60%代码重复（`_resolve_condition`、`_align_condition`、`_sanitize_gene_ids`、`_get_gene_embeddings`、`forward_flow_matching`、`predict`） |
| 3.4 | `magnitudes` 死参数栈 | 整个 DAVF 套件（3+文件） | **0% (死参数)** | 从 `BiPerturbConfig` 到底层编码器全程传递，每一层都被标注 "Kept for API compatibility, but ignored" |
| 3.5 | `pathway_integration.py` 通路图权重 | `pathway_integration.py` | **~70%** | 通路图是完全连接的全零权重，生物学上无意义。依赖 `sspa`（需要 `rpy2`，未在 requirements.txt 中） |
| 3.6 | GenKI VGAE 训练 | `perturbation.py` | **~75%** | 每个预测请求训练10个epoch VGAE，无缓存/检查点。依赖 `torch_geometric` 未列出 |
| 3.7 | 批量预测端点性能 | `routes/predictions.py:188` | **~40% 性能** | **逐个样本 Python for 循环处理**，对1000样本产生1000次独立 GPU 调用 |
| 3.8 | predict / batch_predict 代码重复 | `routes/predictions.py:95-250` | **~40% 复用** | 两个端点共享约60%的预处理/推理/响应构建代码（约20行逐字重复） |
| 3.9 | `lightning_module.py` LoRA 静默失败 | `lightning_module.py:152` | **~90%** | 模型无 `encoder` 属性时仅 warn 不报错，用户无法察觉 LoRA 未应用 |
| 3.10 | `compute_class_weights` 死代码 | `lightning_module.py:181-209` | **0% (死代码)** | 完整实现但**从未被训练管线调用** |
| 3.11 | `CrossModalAttention` 死代码 | `davf_attention.py:246-290` | **0% (死代码)** | 完整实现但**从未被实例化**，注释为"未来扩展" |
| 3.12 | `extended_pathway_kb.py` 孤立数据 | `extended_pathway_kb.py` | **0% (孤立)** | 557行6个结构化数据字典，**从未被任何代码导入** |
| 3.13 | `signaling_network.py:142-146` 显式未实现 | `signaling_network.py` | **~50%** | `_load_pathway_db` 显式抛出 `NotImplementedError` |

---

## 4. 新发现的问题（v1 未覆盖）

| # | 问题 | 文件 | 行号 | 严重级别 |
|---|------|------|------|---------|
| 4.1 | **PTMDataModule 类名冲突** — `datasets.py:125` 定义普通类，`lightning_datamodule.py:20` 定义 Lightning 子类。同名不同类，`__init__.py` 只导出前者 | `datasets.py` + `lightning_datamodule.py` | L125, L20 | **中危** |
| 4.2 | **非标准 AA 映射四重复制** — 同一映射表在 4 个文件中重复定义 | `preprocess.py:46`、`datasets.py:58`、`dataset_base.py`、`multitask_dataset.py:133` | 多处 | **中危** |
| 4.3 | **AA_TO_IDX 五重定义** — 在 `features.py:27`、`datasets.py:55`、`multitask_dataset.py:26`、`ptm_site_dataset.py:16`、`SampleDataset:279` 各定义一次 | 5个文件 | 多处 | **低危** |
| 4.4 | **compute_sample_weights 与 compute_class_weights 重复** — `augmentation.py:181` 和 `dataset_base.py:232` 几乎相同逻辑 | `augmentation.py` + `dataset_base.py` | L181, L232 | **低危** |
| 4.5 | **19/22 脚本无随机种子设置** — 仅 `train_multitask_ptm.py`、`homology_split.py`、`validate_cptac.py` 设置种子 | 全部 `scripts/` | 各处 | **中危** |
| 4.6 | **14/22 脚本无 try/except** — 核心脚本在异常时直接崩溃 | 全部 `scripts/` | 各处 | **中危** |
| 4.7 | **22 个 configs/ 文件未被 git 跟踪** — `.gitignore` 白名单未包含 `configs/` | `.gitignore` | L1-14 | **高危**（已存在于 v1 3.2.3） |
| 4.8 | **Docker GPU 配置不可用** — `python:3.10-slim` 基镜像无 CUDA | `Dockerfile` + `docker-compose.yml` | L2 | **高危**（已存在于 v1 3.2.4） |
| 4.9 | **`preprocess.py` 使用绝对导入** — `from src.utils.helpers` 而非相对导入 | `preprocess.py:13-14` | L13-14 | **低危** |
| 4.10 | **`multitask_dataset.py` 使用 `logging.getLogger`** — 而非项目 `setup_logger` | `multitask_dataset.py` | L16 | **低危** |
| 4.11 | **`ptm_site_dataset.py` 使用 `print()` 而非 `logger`** | `ptm_site_dataset.py` | L81-83, 254 | **低危** |
| 4.12 | **`load_from_json` 无大小限制** — 任意大 JSON 可导致 OOM | `loaders.py:108` | L108 | **低危** |
| 4.13 | **`homology_splitter.py` 仅检查前100样本的无泄漏** — `verify_no_leakage` 只检查 `[:100]` | `homology_splitter.py` | L395 | **低危** |
| 4.14 | **`extract_kmer_features` k=5 时 OOM 风险** — 创建 20^5=3.2M 大小的张量 | `features.py` | L80 | **低危** |
| 4.15 | **无 `.dockerignore`** — 构建上下文可能包含 secrets 或 `__pycache__` | 项目根目录 | 不存在 | **低危** |
| 4.16 | **`UniProtMapper` 依赖未在 requirements/中列出** | `setup.py` + `requirements.txt` | — | **中危** |

---

## 5. 技术债分类清单

### 5.1 代码质量技术债

#### 5.1.1 `mypy: ignore-errors` 全局禁用（高危）

| 文件 | 行号 | 指令 |
|------|------|------|
| `src/models/encoders.py` | L1 | `# mypy: ignore-errors` |
| `src/models/architectures.py` | L1 | `# mypy: ignore-errors` |
| `src/models/pooling.py` | L1 | `# mypy: ignore-errors` |
| `src/models/model_utils.py` | L1 | `# mypy: ignore-errors` |
| `src/models/long_sequence.py` | L1 | `# mypy: ignore-errors` |
| `src/models/multitask_ptm.py` | L1 | `# mypy: ignore-errors` |
| `src/api/routes/predictions.py` | L1 | `# mypy: ignore-errors` |
| `src/analysis/variant_workflow.py` | L1 | `# mypy: ignore-errors` |
| `src/api/routes/state.py` | L1 | `# mypy: ignore-errors` |

**额外 `mypy: disable-error-code`（较窄但仍在抑制）**:

| 文件 | 行号 |
|------|------|
| `src/data/augmentation.py` | L6 |
| `src/data/multitask_dataset.py` | L1 |
| `src/data/ptm_site_dataset.py` | L7 |
| `src/models/multitask.py` | L6 |
| `src/models/ptm_site_predictor.py` | L6 |
| `src/models/signaling_network.py` | L6 |
| `src/models/variant_effect.py` | L6 |
| `src/analysis/pathway_integration.py` | L2 |

**合计**: 9 个 `ignore-errors` + 8 个 `disable-error-code` = **17 个文件** 有 mypy 抑制（v1 报告仅记录了 7 个，严重低估）

#### 5.1.2 代码重复

| # | 问题 | 位置 | 严重级别 |
|---|------|------|---------|
| 5.1.2.1 | 非标准 AA 映射四重复制 | `preprocess.py:46` / `datasets.py:58` / `dataset_base.py` / `multitask_dataset.py:133` | 中 |
| 5.1.2.2 | `DAVF` 与 `LatentDAVF` ~60% 代码重复 | `davf.py` vs `latent_davf.py`（6+ 方法近乎相同） | 高 |
| 5.1.2.3 | `compute_sample_weights` 与 `compute_class_weights` | `augmentation.py:181` / `dataset_base.py:232` | 低 |
| 5.1.2.4 | predict 与 batch_predict ~60% 代码重复（~20行逐字） | `routes/predictions.py:114-145` vs `:192-233` | 中 |
| 5.1.2.5 | AA_TO_IDX 模块级+类级五重定义 | `features.py:27` / `datasets.py:55` / `multitask_dataset.py:26` / `ptm_site_dataset.py:16` / `SampleDataset:279` | 低 |
| 5.1.2.6 | `_standardize_sequence` 逻辑重复 | `datasets.py:62-66` / `ESMTokenizedDataset` L299-303 | 低 |
| 5.1.2.7 | `_load_checkpoint` 加载逻辑重复 | `davf_inference.py:214-231` / `davf_checkpoint_utils.py:93-96` | 低 |

#### 5.1.3 代码规范/风格

| # | 问题 | 位置 | 严重级别 |
|---|------|------|---------|
| 5.1.3.1 | `preprocess.py` 使用绝对导入 | `preprocess.py:13-14` | 低 |
| 5.1.3.2 | `multitask_dataset.py` 使用 `logging.getLogger` 非 `setup_logger` | `multitask_dataset.py:16` | 低 |
| 5.1.3.3 | `ptm_site_dataset.py` 使用 `print()` 非 `logger` | `ptm_site_dataset.py:81-83,254` | 低 |
| 5.1.3.4 | `load_sample_data` 使用 `import random` 行内导入 | `loaders.py:135` | 低 |
| 5.1.3.5 | 中英文文档字符串混用 | 多处 | 低 |
| 5.1.3.6 | `assert self.head_dim * num_heads == hidden_dim` 生产环境被 `-O` 优化 | `pooling.py:37` | 低 |

### 5.2 架构技术债

| # | 问题 | 位置 | 严重级别 |
|---|------|------|---------|
| 5.2.1 | `setup.py` 仅列 10 个包，requirements.txt 有 24+ 个 | `setup.py:14-26` | **高危** |
| 5.2.2 | `configs/` 22 个 YAML 文件未被 git 跟踪 | `.gitignore` | **高危** |
| 5.2.3 | Docker GPU 配置不可用（CPU 基镜像） | `Dockerfile` + `docker-compose.yml` | **高危** |
| 5.2.4 | 编码器选择 if/elif 硬编码（圈复杂度 ~20） | `architectures.py:78-140` | 中 |
| 5.2.5 | `PTMDataModule` 类名冲突 | `datasets.py:125` vs `lightning_datamodule.py:20` | 中 |
| 5.2.6 | `magnitudes` 死参数贯穿 3+ 文件 | DAVF 套件 | 中 |
| 5.2.7 | 通路数据三重复制 | `extended_pathway_kb.py` / `signaling_network.py` / `pathway_integration.py` | 中 |
| 5.2.8 | `Config` 类无模式验证 | `src/utils/config.py` | 中 |
| 5.2.9 | `PerturbationGNN` 名不副实（仅为 per-gene MLP） | `biperturb.py:333-392` | 低 |
| 5.2.10 | `src/api/routes.py` 弃用存根 | `routes.py`（19行重新导出） | 低 |
| 5.2.11 | `no_init_weights` ad-hoc 属性协议 | `davf.py` | 低 |
| 5.2.12 | `z_0` 参数被接受但忽略 | `davf_inference.py` | 低 |

### 5.3 性能技术债

| # | 问题 | 位置 | 严重级别 |
|---|------|------|---------|
| 5.3.1 | Mamba SSM Python for 循环（逐元素，非 associative scan） | `mamba_encoder.py:164-174` | **高危** |
| 5.3.2 | 批量预测端点逐个样本 Python for 循环 | `routes/predictions.py:188` | **高危** |
| 5.3.3 | 自定义 Trainer 无混合精度（AMP） | `trainers.py:107-160` | 中 |
| 5.3.4 | `clean_sequences` 行级 `.items()` 迭代 | `preprocess.py:112-128` | 中 |
| 5.3.5 | `_get_gene_embeddings` Python 级 B×K 循环 | `davf.py:746-757` / `latent_davf.py:443-482` | 中 |
| 5.3.6 | `_build_null_distribution` O(n×m) 无进度指示器 | `significance.py:227-276` | 中 |
| 5.3.7 | DataFrame 重复 `.copy()` | `preprocess.py:72,108,151` | 低 |
| 5.3.8 | `standardize_amino_acids` O(6×n) 多次 replace | `preprocess.py:75-85` | 低 |
| 5.3.9 | `extract_kmer_features` k=5 时 3.2M 张量 OOM 风险 | `features.py:80` | 低 |
| 5.3.10 | `extract_onehot_sequence` 每样本 84KB 密集张量 | `features.py:58` | 低 |
| 5.3.11 | `homology_splitter` O(n²×L) 相似度矩阵 | `homology_splitter.py:138` | 低 |
| 5.3.12 | `Config.to_dict()` 每次 deep copy | `model_utils.py:114` | 低 |

### 5.4 安全技术债

| # | 问题 | 位置 | 严重级别 |
|---|------|------|---------|
| 5.4.1 | `pickle.load()` 无白名单 — 任意代码执行 | `validation.py:226` | **高危** |
| 5.4.2 | `pickle.load()` 无白名单 — 通路缓存 | `pathway_integration.py:103` | **高危** |
| 5.4.3 | `pickle.load()` 无白名单 — 通路缓存 | `pathway_integration.py:151` | **高危** |
| 5.4.4 | `weights_only=False` 回退 — 不安全加载 | `davf_inference.py:227-231` | 中 |
| 5.4.5 | `weights_only=False` 回退（重复） | `davf_checkpoint_utils.py:93-96` | 中 |
| 5.4.6 | CORS `allow_methods=["*"], allow_headers=["*"]` 过于宽松 | `app.py:62-63` | 中 |
| 5.4.7 | `load_from_json` 无大小限制 | `loaders.py:108` | 低 |
| 5.4.8 | Docker 以 root 运行，无 `USER appuser` | `Dockerfile` | 低 |
| 5.4.9 | 无 `.dockerignore` | 项目根目录 | 低 |

### 5.5 测试技术债

| # | 问题 | 位置 | 严重级别 |
|---|------|------|---------|
| 5.5.1 | GPU-only 测试 — CI 永不被执行 | `tests/unit/test_models.py:403-406` 等处 | **高危** |
| 5.5.2 | 测试只检查 `is not None` 不验证内容 | `tests/unit/test_data.py:279-286` | **高危** |
| 5.5.3 | `TestPTM2CellNetWithPretrained` 过度 Mock + GPU-skip | `tests/unit/test_models.py:344-466` | **高危** |
| 5.5.4 | `test_fast_dev_run` 空操作 | `tests/unit/training/test_lightning_module.py:476` | **高危** |
| 5.5.5 | 异常处理中空 `pass` | `tests/unit/test_train_pretrained_script.py:69,129,139` | 中 |
| 5.5.6 | 边缘情况缺失（空输入/NaN/极值） | 多数测试文件 | 中 |
| 5.5.7 | LoRA 测试 mock `_apply_lora` 全部 | `tests/unit/training/test_lightning_module.py:419-453` | 中 |
| 5.5.8 | 回归评估指标未测试 | `tests/unit/evaluation` | 中 |
| 5.5.9 | `UndefinedMetricWarning` 全局抑制 | `src/evaluation/metrics.py:46-47` | 中 |
| 5.5.10 | 随机种子未隔离 | `tests/unit/evaluation/test_metrics.py:189-195` | 低 |
| 5.5.11 | `NamedTemporaryFile(delete=False)` 泄露 | `tests/unit/test_data.py:35-45` | 低 |
| 5.5.12 | `pytest.ini` 定义但未使用的 markers（gpu/slow/integration） | `pytest.ini` | 低 |

### 5.6 依赖管理技术债

| # | 问题 | 位置 | 严重级别 |
|---|------|------|---------|
| 5.6.1 | `setup.py` 与 `requirements.txt` 严重不一致 | `setup.py:14-26` | **高危** |
| 5.6.2 | `torch_geometric` 缺失（GenKI 必需） | `perturbation.py:270` | **高危** |
| 5.6.3 | `scanpy` 缺失（GenKI `genki_source` 必需） | `reference_data.py:101` | **高危** |
| 5.6.4 | `lightning>=2.5.0` 与 `torch>=1.10.0` 冲突 | `requirements.txt` | **高危** |
| 5.6.5 | `UniProtMapper` 未在任何文件中列出 | 代码导入处 | 中 |
| 5.6.6 | `safetensors` 未显式声明（代码直接使用） | `geneformer_embedding.py:111,144` | 中 |
| 5.6.7 | `rpy2` 缺失（SSPA 间接依赖） | `pathway_integration.py:11` | 中 |
| 5.6.8 | `pydantic>=1.8.0` 与 FastAPI 0.100+ 不兼容 | `requirements.txt` | 中 |
| 5.6.9 | `pytest` 不在任何依赖列表中 | `requirements.txt` + `setup.py` | 中 |
| 5.6.10 | `torchvision>=0.11.0` 等3包被列入但从未导入 | `requirements.txt` | 低 |
| 5.6.11 | 可选依赖未标记 | `requirements.txt` | 低 |
| 5.6.12 | 无 `extras_require` / 无 `entry_points` | `setup.py` | 低 |
| 5.6.13 | `weight_decay` 百倍差异（`1e-4` vs `0.01`） | `configs/default.yaml` vs 其他 | 低 |

---

## 6. 风险评估矩阵

### 6.1 按严重级别汇总

| 类别 | 高危 | 中危 | 低危 | 合计 |
|------|------|------|------|------|
| **未实现功能（修正后）** | 4 (外部工具存根/训练器缺失) | 8 (DB集成/脚本健壮性) | 8 (空目录/配置) | 20 |
| **未完全实现** | 3 (死参数栈/性能bug/代码重复) | 7 (通路/GNN/VGAE/类名冲突) | 7 (死代码/孤立数据) | 17 |
| **代码质量债** | 9 (mypy全局抑制) | 4 (代码重复) | 8 (风格/导入/print) | 21 |
| **架构债** | 3 (setup/configs/Docker) | 6 (类名冲突/死参数/通路) | 4 (存根/ad-hoc) | 13 |
| **性能债** | 2 (Mamba循环/单样本批处理) | 4 (AMP/行级迭代/null分布) | 6 (copy/magic/OOM) | 12 |
| **安全债** | 3 (pickle RCE) | 3 (unsafe加载/CORS) | 2 (root运行/dockerignore) | 8 |
| **测试债** | 4 (GPU-skip/空断言/过度mock) | 5 (空pass/边缘缺失/抑制) | 3 (种子泄漏/未用marker) | 12 |
| **依赖债** | 4 (setup冲突/3包缺失) | 5 (未列出/不兼容) | 4 (未用/未标记) | 13 |
| **合计** | **32** | **42** | **42** | **116** |

### 6.2 v1 与 v2 差异总览

| 指标 | v1 | v2 | 差异原因 |
|------|----|----|---------|
| "未实现"条目 | 27 | 20 | v1 有 8 条伪阳性（已证实存在） |
| mypy 抑制文件 | 7 | 17 | v1 遗漏了大量 `disable-error-code` 变体 |
| 总问题数 | 148 | 116 | 去除伪阳性 + 新增发现（脚本种子, try/except 等） |

---

## 7. 优先级修复建议

### P0 — 立即修复（生产阻塞）

| 优先级 | 问题 # | 描述 | 建议修复 |
|--------|--------|------|---------|
| P0 | 5.4.1-3 | `pickle.load()` 无白名单 (RCE) | 替换为 `safetensors` 或 JSON，或使用严格白名单的 Unpickler |
| P0 | 5.6.1 | `setup.py` 依赖严重缺失 | 同步 `setup.py` 的 `install_requires` 与 `requirements.txt` |
| P0 | 5.6.4 | `lightning>=2.5.0` 与 `torch>=1.10.0` 冲突 | 设置 `torch>=2.1.0` |
| P0 | 2.3.1-2 | 自定义 Trainer 无梯度裁剪/AMP | 添加 `clip_grad_norm_()` 和 `cuda.amp` 支持 |

### P1 — 高优先级

| 优先级 | 问题 # | 描述 | 建议修复 |
|--------|--------|------|---------|
| P1 | 5.2.2 | `configs/` 未跟踪 | 在 `.gitignore` 添加 `!configs/` 豁免 |
| P1 | 5.2.3 | Docker GPU 配置不可用 | 基于 `nvidia/cuda:12.x-runtime-ubuntu22.04` 构建 |
| P1 | 5.3.2 | 批量预测逐个样本 | 改为真正 batched 张量处理 |
| P1 | 5.1.1 | 17个源文件 mypy 抑制 | 逐个修复类型错误后移除全局忽略 |
| P1 | 5.3.1 | Mamba SSM Python 循环 | 使用 `torch.associative_scan` 替代 for 循环 |
| P1 | 5.5.1-4 | GPU-only 测试 / 空断言 / 过度 mock | 添加 CPU 兼容回退或条件测试 |
| P1 | 4.5 | 19/22 脚本无种子设置 | 添加 `torch.manual_seed` 和 `np.random.seed` |
| P1 | 5.2.4 | 编码器 if/elif 硬编码 | 引入注册模式 |

### P2 — 中优先级

| 优先级 | 问题 # | 描述 | 建议修复 |
|--------|--------|------|---------|
| P2 | 3.3 | DAVF/LatentDAVF 60%重复 | 提取公共基类或 mixin |
| P2 | 3.4 | `magnitudes` 死参数 | 移除贯穿3文件的死参数 |
| P2 | 3.7-8 | predict/batch_predict 代码重复 | 提取共享推理函数 |
| P2 | 5.4.6 | CORS 过于宽松 | 限制 `allow_methods` 和 `allow_headers` |
| P2 | 5.6.2-3 | 缺失 `torch_geometric`/`scanpy` | 添加到 requirements.txt + clean error |
| P2 | 4.1 | PTMDataModule 类名冲突 | 重命名其中一个，添加清晰导入说明 |
| P2 | 4.2-3 | AA 映射四重复制/AA_TO_IDX 五重定义 | 提取到 `src/data/constants.py` |
| P2 | 5.6.9 | pytest 不在依赖中 | 添加 `extras_require["dev"]` |
| P2 | 3.5 | 通路图全零权重 | 实现真实通路图或添加 TODO |

### P3 — 低优先级

| 优先级 | 问题 # | 描述 |
|--------|--------|------|
| P3 | 2.2.1-4 | 外部工具存根（AlphaFold/BLAST/ClustalW/PSIPRED） |
| P3 | 3.10-11 | 死代码清理（`compute_class_weights`, `CrossModalAttention`） |
| P3 | 3.12 | `extended_pathway_kb.py` 接入或移除 |
| P3 | 5.1.3 | 导入/日志/print 风格统一 |
| P3 | 5.3.7-8 | DataFrame 操作向量化优化 |
| P3 | 2.1.1 | UniProt API 集成 |
| P3 | 2.1.3 | 特征选择与降维（PCA/UMAP） |
| P3 | 2.5.4-6 | 脚本种子 / --resume / try/except |
| P3 | 2.1.4 | 在线数据增强集成 |
| P3 | 5.4.8-9 | Docker 安全加固 |
| P3 | 5.6.10-12 | setup.py extras_require / entry_points |

---

*报告版本: v2 | 生成方式: ODW并行Agent调研 + 逐文件源代码验证 | 覆盖 95+ 个源文件*
