# Mamba超参数调优报告

**日期**: 2026-03-12
**目标**: 找到Mamba模型的最优超参数配置
**状态**: 🔄 进行中

---

## 实验设计

### 搜索空间

| 超参数 | 搜索范围 | 说明 |
|--------|----------|------|
| `state_dim` | [4, 8, 16, 32] | 状态空间维度 |
| `expand_factor` | [1, 2] | 扩展因子 |
| `learning_rate` | [5e-5, 1e-4, 2e-4, 5e-4] | 学习率 |

### 精选组合（快速调优）

基于经验和计算资源限制，选择以下4组最有希望的组合：

| # | state_dim | expand_factor | learning_rate | 预期效果 |
|---|-----------|---------------|---------------|----------|
| 1 | 4 | 1 | 2e-4 | 最小模型，快速训练 |
| 2 | 8 | 1 | 2e-4 | 较小状态空间 |
| 3 | 16 | 1 | 1e-4 | 降低学习率，更稳定 |
| 4 | 16 | 2 | 5e-5 | 更大扩展，更低学习率 |

### 固定配置

```yaml
hidden_dim: 256
num_layers: 6
dropout: 0.2  # 增加dropout防止过拟合
batch_size: 16
max_epochs: 10
optimizer: adamw
weight_decay: 0.01
scheduler: cosine
warmup_epochs: 3
```

---

## 实验进度

| 实验 | 配置 | 状态 | 开始时间 | 预计完成 |
|------|------|------|----------|----------|
| #1 | sd=4, ef=1, lr=2e-4 | 🔄 运行中 | 08:19 | ~10:00 |
| #2 | sd=8, ef=1, lr=2e-4 | ⏳ 等待 | - | - |
| #3 | sd=16, ef=1, lr=1e-4 | ⏳ 等待 | - | - |
| #4 | sd=16, ef=2, lr=5e-5 | ⏳ 等待 | - | - |

---

## 基线对比

当前最佳基线（state_dim=16, expand_factor=2, lr=2e-4）：
- 最佳验证损失: 0.7899
- 测试准确率: 20.75%
- 训练时间: ~90分钟

**目标**:
- 验证损失 < 0.78
- 测试准确率 > 25%
- 减少过拟合

---

## 调优脚本

**位置**: `scripts/tune_mamba_hyperparams.py`

**使用方法**:
```bash
# 完整网格搜索（24组，约36小时）
python scripts/tune_mamba_hyperparams.py

# 快速调优（4组，约6-8小时）
python /tmp/quick_tune.py
```

**输出位置**:
- 配置文件: `outputs/hyperparam_tuning/*.yaml`
- 结果: `/tmp/quick_tune_results.json`

---

## 预期结果

### 成功标准
1. 至少一组配置验证损失 < 0.78
2. 测试准确率比基线提升 > 5%
3. 找到最优超参数组合

### 后续行动
- 使用最优配置进行100 epoch完整训练
- 对比调优前后的性能提升
- 更新benchmark报告

---

*报告生成时间*: 2026-03-12 08:20
*调优任务ID*: bgh2drxyj
