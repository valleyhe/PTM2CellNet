# PTM2CellNet 当前代码现状系统性复核报告 v12

生成日期: 2026-07-05  
复核对象: 本地 `main` 分支 `eaf08a9`  
前置集成状态: 当前新增代码修改已提交到本地 `main`:

- `62c0c36 chore: integrate latest code updates before audit`
- `eaf08a9 test: clean latest lint regressions`

本报告替代 v11。v11 的部分结论已经过期: safe checkpoint unsafe fallback、roadmap 职责混杂、AlphaFold fallback confidence、GenKI batch 默认串行、ruff 失败等均已发生变化。

## 1. 复核方法与证据范围

本报告基于当前代码、CodeGraph 索引、测试执行、静态检查和关键文件行级阅读。

| 证据项 | 当前结果 |
|---|---|
| CodeGraph 索引 | 289 个文件、5980 个符号、6132 条边 |
| Python 源码规模 | `src/` 120 个 Python 文件 |
| 脚本规模 | `scripts/` 33 个 Python 文件 |
| 测试文件规模 | 115 个 `test*.py`，其中 unit 95、integration 8、e2e 7、根级测试 5 |
| 测试收集 | `pytest --collect-only -q`: 1593 tests collected |
| 全量测试 | `pytest -q`: 1587 passed, 6 skipped, 20 warnings in 441.91s |
| 目标回归测试 | 157 passed, 6 warnings |
| Ruff | `ruff check src scripts tests --statistics`: 通过，无输出 |
| Mypy | `mypy src`: 43 errors in 22 files |

已执行命令:

```bash
git add -A && git commit -m "chore: integrate latest code updates before audit"
ruff check src scripts tests --statistics
pytest -q tests/unit/test_safe_io.py tests/unit/test_middleware.py tests/unit/test_predictions_routes.py
git add ... && git commit -m "test: clean latest lint regressions"
mypy src
pytest --collect-only -q
pytest -q
```

## 2. 总体结论

当前项目已进入高成熟度阶段。核心训练/推理/API/集成测试均可完整运行，全量测试已经通过，ruff 高信号规则已清零，mypy 从 v11 的 181 错误进一步降至 43 错误。

自 v11 后的关键变化:

- safe checkpoint 的 unsafe fallback 已从库路径移除。`DAVFInferenceModule` 现在只通过 `safe_torch_load()` 的 safe path 加载，legacy checkpoint 失败时提示迁移工具，而不是自动 `weights_only=False` fallback。[src/models/davf_inference.py:342](../src/models/davf_inference.py#L342), [src/models/davf_inference.py:347](../src/models/davf_inference.py#L347), [src/models/davf_inference.py:352](../src/models/davf_inference.py#L352)
- 专门新增 `scripts/tools/migrate_legacy_checkpoint.py` 作为唯一允许 `torch.load(weights_only=False)` 的受控迁移工具。[scripts/tools/migrate_legacy_checkpoint.py:5](../scripts/tools/migrate_legacy_checkpoint.py#L5), [scripts/tools/migrate_legacy_checkpoint.py:41](../scripts/tools/migrate_legacy_checkpoint.py#L41), [scripts/tools/migrate_legacy_checkpoint.py:72](../scripts/tools/migrate_legacy_checkpoint.py#L72)
- `roadmap.py` 已变成兼容聚合层，取消功能的实代码迁移到 `src/data/loaders/legacy_loaders.py`，`launch_gui()` 已从导出层移除。[src/models/roadmap.py:1](../src/models/roadmap.py#L1), [src/models/roadmap.py:6](../src/models/roadmap.py#L6), [src/models/roadmap.py:31](../src/models/roadmap.py#L31)
- GenKI batch 默认改为并行执行，保留顺序路径作为可选项。[src/integration/genki/perturbation.py:282](../src/integration/genki/perturbation.py#L282), [src/integration/genki/perturbation.py:286](../src/integration/genki/perturbation.py#L286), [src/integration/genki/perturbation.py:331](../src/integration/genki/perturbation.py#L331)
- AlphaFold fallback confidence 已从 0.5 降到 0.0，并继续标记 `model_kind="synthetic_fallback"`。[src/models/external_tools/alphafold.py:106](../src/models/external_tools/alphafold.py#L106), [src/models/external_tools/alphafold.py:112](../src/models/external_tools/alphafold.py#L112), [src/models/external_tools/alphafold.py:129](../src/models/external_tools/alphafold.py#L129)
- `_DatasetConfig` 已替换/收敛为 `DatasetConfig` dataclass，大量 Dataset union 类型错误已经消除。[src/data/dataset_base.py:47](../src/data/dataset_base.py#L47), [src/data/dataset_base.py:53](../src/data/dataset_base.py#L53), [src/data/dataset_base.py:128](../src/data/dataset_base.py#L128)

仍需关注的问题集中在 4 类:

1. 生产语义: DAVF E2E 微调仍明确是实验性脚本；ESM-3/Geneformer/AlphaFold 等外部模型能力仍需要 provenance 和 strict production gate。
2. 类型质量: mypy 仍有 43 个错误，主要是缺失 stubs、未必要的 `type: ignore`、部分 Tensor/Module union 类型、稀疏矩阵类型和 matplotlib stub 参数。
3. 取消功能兼容 shim: 实时质谱流和自定义 PTM 数据库已移入 legacy loaders，但功能仍可调用，需确保文档只把它们描述为 legacy import shim。
4. 文档同步: v10/v11 与 v12 并存，旧报告结论会被搜索命中，需要权威索引页。

## 3. 未实现或未完全实现功能清单

### 3.1 仍影响交付可信度的功能缺口

| ID | 功能项 | 当前完成度 | 缺失关键组件 / 不符合规范处 | 证据 |
|---|---:|---:|---|---|
| F-01 | DAVF E2E 微调生产化 | 35% | 脚本已 fail-fast 标记 experimental，需显式 `PTM2CELLNET_ALLOW_EXPERIMENTAL=1` 才能运行；仍使用 placeholder `DownstreamHead`、简化字符编码和单一 `label` 列，未接入项目正式 Dataset/manifest/artifact 契约 | 实验 guard。[scripts/finetune_davf_e2e.py:5](../scripts/finetune_davf_e2e.py#L5), [scripts/finetune_davf_e2e.py:31](../scripts/finetune_davf_e2e.py#L31)；placeholder head。[scripts/finetune_davf_e2e.py:94](../scripts/finetune_davf_e2e.py#L94)；简化编码。[scripts/finetune_davf_e2e.py:165](../scripts/finetune_davf_e2e.py#L165) |
| F-02 | ESM-3 真实生产集成 | 70% | `ESM3Encoder` 类存在并可接收 structure/function tokens，但真实 ESM-3 权重、tokenizer、结构 token 合约和离线/生产 strict 矩阵仍未通过本次验证 | ESM3Encoder 定义与模型名映射。[src/models/pretrained_encoders.py:363](../src/models/pretrained_encoders.py#L363), [src/models/pretrained_encoders.py:370](../src/models/pretrained_encoders.py#L370)；多模态 forward 参数。[src/models/pretrained_encoders.py:459](../src/models/pretrained_encoders.py#L459), [src/models/pretrained_encoders.py:484](../src/models/pretrained_encoders.py#L484) |
| F-03 | 分布式训练闭环 | 50% | Helper 可构造 Lightning DDP trainer，但仍未证明训练脚本统一接入、多进程/多 GPU、分布式 sampler、checkpoint 聚合和 artifact 一致性 | scope 仍标记 deferred。[src/project/scope.py:45](../src/project/scope.py#L45)；helper DDP 分支。[src/training/distributed.py:53](../src/training/distributed.py#L53), [src/training/distributed.py:60](../src/training/distributed.py#L60), [src/training/distributed.py:80](../src/training/distributed.py#L80) |
| F-04 | Geneformer 生产嵌入可信度 | 80% | 已有 strict 和 provenance，但默认非 strict 仍可随机 fallback；生产配置必须强制 strict，并在 API/artifact 中外显 provenance | strict 失败路径。[src/models/geneformer_embedding.py:235](../src/models/geneformer_embedding.py#L235)；fallback 随机 embedding。[src/models/geneformer_embedding.py:254](../src/models/geneformer_embedding.py#L254), [src/models/geneformer_embedding.py:267](../src/models/geneformer_embedding.py#L267)；provenance。[src/models/geneformer_embedding.py:369](../src/models/geneformer_embedding.py#L369), [src/models/geneformer_embedding.py:389](../src/models/geneformer_embedding.py#L389) |
| F-05 | 通路验证生产化 | 70% | 内置通路可用，外部验证失败会返回空报告或 builtin-only；需要生产数据包/缓存和明确的 validation provenance | 失败返回空。[src/models/signaling_network.py:581](../src/models/signaling_network.py#L581)；未验证但有内置通路时返回 `builtin_only`。[src/models/signaling_network.py:592](../src/models/signaling_network.py#L592), [src/models/signaling_network.py:597](../src/models/signaling_network.py#L597) |

### 3.2 已取消但仍保留兼容代码的功能

这些不应再作为 roadmap 目标，但兼容入口仍存在。

| 功能 | 当前状态 | 风险 | 证据 |
|---|---|---|---|
| 实时质谱流 | `mass_spec_stream()` 已从 `roadmap.py` 移到 `legacy_loaders.py`，仍能解析 TSV 并 warning | 用户可能把 legacy shim 当成正式实时流能力 | 取消登记。[src/project/scope.py:63](../src/project/scope.py#L63)；legacy loader。[src/data/loaders/legacy_loaders.py:63](../src/data/loaders/legacy_loaders.py#L63), [src/data/loaders/legacy_loaders.py:71](../src/data/loaders/legacy_loaders.py#L71) |
| 自定义 PTM 数据库 | `load_custom_ptm_database()` 已迁移到 legacy loader，仍可读 CSV/TSV/JSON/Excel | 与“用户托管自定义 PTM 数据库/API 已取消”容易混淆 | 取消登记。[src/project/scope.py:73](../src/project/scope.py#L73)；legacy loader。[src/data/loaders/legacy_loaders.py:91](../src/data/loaders/legacy_loaders.py#L91), [src/data/loaders/legacy_loaders.py:105](../src/data/loaders/legacy_loaders.py#L105) |
| GUI | `launch_gui()` 已不再从 `roadmap.py` 导出 | 风险已显著降低，只需清理旧文档 | roadmap 说明 removed。[src/models/roadmap.py:6](../src/models/roadmap.py#L6), [src/models/roadmap.py:11](../src/models/roadmap.py#L11)；取消登记。[src/project/scope.py:83](../src/project/scope.py#L83) |
| API key 功能扩展 | 新扩展取消，现有 middleware/initialize 认证保留 | 不应继续规划新的 API-key feature，只维护现有安全边界 | 取消登记。[src/project/scope.py:92](../src/project/scope.py#L92) |

### 3.3 蓝图接口与当前实现不完全一致

| 契约/需求 | 当前状态 | 建议 |
|---|---|---|
| 模块级 `load_and_preprocess(data_path)` | 能力通过 `DataLoader` + `DataPreprocessor` 组合实现，未见同名模块级入口 | 文档改为当前类接口，或补一层薄封装 |
| 模块级 `extract_features(df)` | 主要是 `FeatureExtractor` 类方法 | 同上 |
| 类方法式 `Evaluator.cross_validate()` | 当前有函数式和类能力，但蓝图接口仍有历史描述差异 | 文档以 `src/evaluation/evaluators.py` 当前实现为准 |
| GUI/实时流/自定义 PTM DB | 已取消，legacy shim 仅为兼容 | 旧报告加历史快照声明 |

## 4. 技术债分类

### 4.1 高严重度

| ID | 类别 | 问题 | 影响 | 解决策略 |
|---|---|---|---|---|
| TD-H1 | 类型质量 | `mypy src` 仍有 43 个错误，当前主要集中于缺失 stubs、unused ignore、Tensor/Module union、稀疏矩阵类型和 matplotlib 参数类型 | 类型检查仍不能作为严格 CI gate | 先确保 CI 安装 `requirements-core.txt` 中新增 stubs；随后分模块清理 unused ignore、middleware 返回类型、visualization tuple 参数、GenKI `_ReferenceData` 类型 |
| TD-H2 | 生产语义 | 外部模型/工具 fallback 仍可能默认流入结果，尤其 Geneformer fallback_random、AlphaFold synthetic_fallback、ESM-3 asset failure | 结果可信度依赖调用方读取 warning/provenance，容易误用 | production config 默认 `PTM2CELLNET_STRICT_MODEL_ASSETS=1`，API `/model/info`、prediction response、artifact manifest 统一输出 provenance |
| TD-H3 | DAVF E2E | 脚本已明确 experimental，但仍放在正式 `scripts/` 根目录 | 用户可能以为它是生产训练入口 | 移到 `scripts/experimental/` 或完成正式 DataModule/artifact/manifest 契约 |

### 4.2 安全债

| ID | 问题 | 当前证据 | 建议 |
|---|---|---|---|
| TD-S1 | unsafe checkpoint 反序列化已从库路径移除，但迁移工具仍是高风险工具 | 迁移脚本明确唯一允许 `weights_only=False`。[scripts/tools/migrate_legacy_checkpoint.py:5](../scripts/tools/migrate_legacy_checkpoint.py#L5), [scripts/tools/migrate_legacy_checkpoint.py:72](../scripts/tools/migrate_legacy_checkpoint.py#L72) | 保持脚本不被 API/训练自动调用；文档标注“只用于完全可信本地文件” |
| TD-S2 | `safe_torch_load()` 仍保留 opt-out 能力 | 函数文档说明可信 legacy 可 `enforce_safe_only=False`，内部仍可 fallback。[src/utils/safe_io.py:229](../src/utils/safe_io.py#L229), [src/utils/safe_io.py:297](../src/utils/safe_io.py#L297) | CI/production 设置 `PTM2CELLNET_SAFE_LOAD_ONLY=1`，并扫描 `src/` 禁止 `enforce_safe_only=False` |
| TD-S3 | pickle 缓存完整性仍需统一 | v11 提到 pathway/data validation 缓存；本轮未专门消除该类设计 | 对缓存引入 HMAC 或迁移到 safetensors/JSON+hash |

### 4.3 架构与代码质量

| ID | 问题 | 证据 | 建议 |
|---|---|---|---|
| TD-A1 | `scripts/finetune_davf_e2e.py` 把实验 guard 放在顶层 import 时执行 | 顶层 guard 在项目 import 前直接 raise。[scripts/finetune_davf_e2e.py:31](../scripts/finetune_davf_e2e.py#L31) | 将 guard 移入 `main()`，避免测试/文档工具 import 时异常；或把脚本迁入 experimental |
| TD-A2 | `legacy_loaders.py` 仍提供完整功能实现而非 hard fail | [src/data/loaders/legacy_loaders.py:63](../src/data/loaders/legacy_loaders.py#L63), [src/data/loaders/legacy_loaders.py:91](../src/data/loaders/legacy_loaders.py#L91) | 保留兼容但从 README/指南删除正式使用示例 |
| TD-A3 | `setup.py` 与 `requirements-core.txt` 依赖边界不完全一致 | `requirements-core` 不拉 transformers/lightning，但 `setup.py install_requires` 仍包含 transformers/lightning。[requirements-core.txt:9](../requirements-core.txt#L9), [setup.py:62](../setup.py#L62), [setup.py:68](../setup.py#L68) | 让 `install_requires` 与 core 对齐，pretrained/lightning 放 extras，避免最小安装过重 |
| TD-A4 | `pydantic` 版本边界不一致 | `requirements-core.txt` 是 `pydantic>=1.10,<3`，setup api extra 是 `pydantic>=2.0.0`。[requirements-core.txt:48](../requirements-core.txt#L48), [setup.py:35](../setup.py#L35) | 明确支持 v2 或 v1/v2 双兼容并补矩阵测试 |

### 4.4 性能债

| ID | 问题 | 证据 | 建议 |
|---|---|---|---|
| TD-P1 | 全量测试耗时 441.91 秒 | 本次 `pytest -q` 结果 | 保留全量 CI，但本地默认提供 fast profile；使用 `pytest --durations=30` 定位慢项 |
| TD-P2 | GenKI 默认并行使用线程池，但缺少并发性能基准和线程安全压力测试 | 默认 `parallel=True`。[src/integration/genki/perturbation.py:286](../src/integration/genki/perturbation.py#L286) | 增加 batch benchmark、结果顺序稳定测试、cache 并发测试 |
| TD-P3 | Mamba 三路径性能缺少基准门禁 | 并行/fused/顺序路径存在，未在本次验证中测性能 | 增加 CPU parallel 与 CUDA fused benchmark |

### 4.5 测试债

| ID | 问题 | 当前状态 | 建议 |
|---|---|---|---|
| TD-T1 | 全量测试通过但耗时较长 | 1587 passed, 6 skipped, 441.91s | 拆分 unit/integration/e2e/slow CI job |
| TD-T2 | 外部服务真实性未验证 | 本次测试主要 mock/本地 fixture，未联网验证 UniProt/AlphaFold/Geneformer/KEGG | 增加 nightly online smoke，允许重试和 skip |
| TD-T3 | DAVF E2E 生产路径无正式测试 | 当前脚本被 experimental guard 拦截，无法作为正式训练路径测试 | 若保持实验脚本，只测 fail-fast；若转正，补 subprocess 1 epoch smoke |

### 4.6 文档债

| ID | 问题 | 影响 | 建议 |
|---|---|---|---|
| TD-D1 | 多版本报告并存 | v10/v11/v12 结论相互覆盖，搜索结果容易误导 | 新增 `docs/CURRENT_STATUS.md` 指向 v12，旧报告标记历史快照 |
| TD-D2 | scope 中 `DEFERRED_FEATURES` 对 ESM-3 写“Implemented in v1.0”但仍在 deferred list | 语义冲突，易误判完成度 | 将 ESM-3 从 deferred 迁到 completed-with-caveats，或改文案为 partial/asset-dependent |
| TD-D3 | 实验脚本错误信息仍指向 v11 报告 | `finetune_davf_e2e.py` guard 提示 v11。[scripts/finetune_davf_e2e.py:39](../scripts/finetune_davf_e2e.py#L39) | 更新为 v12 或 stable current-status 链接 |

## 5. 高严重度问题解决策略

### 5.1 Mypy 43 错误清零

当前 `mypy src` 剩余 43 errors in 22 files。建议按收益排序:

1. 确保 CI 安装 stubs: `types-requests`、`types-PyYAML`、`pandas-stubs` 已在 `requirements-core.txt` 声明，但当前环境仍报缺失，需确认环境安装流程。[requirements-core.txt:64](../requirements-core.txt#L64)
2. 删除/修正 unused `type: ignore`: `src/integration/genki/reference_data.py`、`src/models/external_tools/base.py`、`src/integration/genki/perturbation.py`、`src/api/app.py`。
3. 修 `api/middleware.py` 的 `no-any-return`: 将 header/client host 显式 cast 为 `str`。[src/api/middleware.py:241](../src/api/middleware.py#L241)
4. 修 `visualization.py` set_xlim/set_ylim: 用 tuple `(0.0, 1.0)` 替代 list。[src/evaluation/visualization.py:367](../src/evaluation/visualization.py#L367), [src/evaluation/visualization.py:411](../src/evaluation/visualization.py#L411)
5. 修 GenKI reference 类型: `_run_batch_parallel()` / `_run_with_shared_reference()` 接收 `_ReferenceData` 而非裸 `dict`。[src/integration/genki/perturbation.py:331](../src/integration/genki/perturbation.py#L331)

完成标准:

```bash
mypy src
```

结果为 0 errors。

### 5.2 外部模型 provenance 统一

执行:

1. 定义统一枚举: `real`, `fallback_random`, `synthetic_fallback`, `substituted_model`, `not_loaded`。
2. Geneformer、ESM3 wrapper、AlphaFold、demo model、DAVF checkpoint failure 全部返回统一 provenance。
3. `/model/info`、prediction response、artifact manifest 输出 provenance。
4. production 默认设置:

```bash
PTM2CELLNET_STRICT_MODEL_ASSETS=1
PTM2CELLNET_SAFE_LOAD_ONLY=1
```

### 5.3 DAVF E2E 决策

二选一:

1. 保持实验: 移到 `scripts/experimental/finetune_davf_e2e.py`，顶层不再 raise，CLI `main()` fail-fast，文档只列为研究入口。
2. 转正: 复用 `DatasetConfig`/DataModule，支持 `sequence/ptm_sites/gene_symbol/label`，保存 manifest、label mapping、dataset hash、loss history，新增 1 epoch subprocess smoke。

## 6. 功能完成度矩阵

| 模块 | 完成度 | 说明 |
|---|---:|---|
| 数据加载/预处理 | 88% | 公共数据源、安全下载、legacy shim 分离已较完整；mypy/stub 仍需收尾 |
| Dataset/特征工程 | 86% | `DatasetConfig` dataclass 已落地，大量类型债已清；仍有少量 feature return 类型债 |
| 核心模型 | 87% | PTM2CellNet、Mamba、pretrained encoder 可测；ESM-3 真实资产未验证 |
| DAVF/Geneformer | 80% | 安全加载改进明显，Geneformer strict/provenance 已有；DAVF E2E 仍实验 |
| 训练 | 82% | 原生/Lightning/自监督/LoRA 可用；分布式未生产验证 |
| 评估/解释 | 86% | evaluate/cross_validate、可视化、TwoStage pipeline 可用；少量 mypy 债 |
| API | 88% | 初始化认证、预测、middleware、metrics 测试覆盖增强；类型债剩余 |
| GenKI/集成 | 87% | 默认并行、合约和测试较完整；需性能/线程安全基准 |
| 外部工具 | 72% | AlphaFold/PSIPRED/BLAST/ClustalW 客户端有 fallback；生产 provenance 需统一 |
| 测试 | 90% | 全量 1587 passed；耗时长但结果可信 |
| 静态质量 | 82% | ruff 通过，mypy 剩 43 errors |
| 文档 | 72% | 报告丰富但版本多，需要当前状态入口 |

整体完成度估计: 86%。

## 7. 验证记录

```bash
pytest --collect-only -q
# 1593 tests collected

pytest -q
# 1587 passed, 6 skipped, 20 warnings in 441.91s

pytest -q tests/unit/test_safe_io.py tests/unit/test_middleware.py tests/unit/test_predictions_routes.py
# 66 passed, 3 warnings

pytest -q tests/unit/test_safe_io.py tests/unit/test_middleware.py tests/unit/test_predictions_routes.py \
  tests/unit/test_roadmap.py tests/unit/test_davf_inference.py \
  tests/unit/test_davf_foundation_regressions.py tests/unit/test_external_tools.py \
  tests/unit/test_signaling_network_kegg.py
# 157 passed, 6 warnings

ruff check src scripts tests --statistics
# passed, no output

mypy src
# 43 errors in 22 files
```

## 8. 风险与边界条件

- 本次未联网验证 UniProt、AlphaFold、Geneformer/ESM-3 权重、KEGG/Reactome 真实服务。
- 全量测试通过证明本地当前环境可用，但不证明 GPU/Mamba fused、多 GPU DDP 或外部服务路径。
- mypy 未通过，静态类型门禁仍不能作为严格发布 gate。
- `weights_only=False` 仍存在于迁移工具和部分脚本测试/训练 resume 场景，生产环境必须限制调用边界。

## 9. 置信度

置信度: 高。

依据:

- 当前 HEAD 的全部未提交代码已提交。
- CodeGraph、测试收集、全量测试、ruff、mypy 均已在当前代码上实际执行。
- 报告中的主要判断均有当前源码行级引用。
