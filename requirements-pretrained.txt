# Pretrained protein language models (ESM-2 / ProtBERT) — P1-2.
#
# Install on top of core to enable ``scripts/train_pretrained.py`` and the
# pretrained encoders:
#     pip install -r requirements-core.txt -r requirements-pretrained.txt
#
# These are kept out of the default install because they pull in the Hugging
# Face stack and are only needed for the real-model training path.

-r requirements-core.txt

transformers>=4.20.0
peft>=0.8.0
einops>=0.6.0
# Lightning is required for the pretrained/Lightning training entrypoints.
lightning>=2.5.0
# torchmetrics is required for Lightning training and the AUPR-torch metric.
# evaluation/metrics.py falls back to sklearn when torchmetrics is absent,
# so core does not declare it — only pretrained (Lightning) needs it.
torchmetrics>=0.11.0
tensorboard>=2.13.0
# ESM-3 support (EvolutionaryScale)
esm>=3.0.0
