# Advanced single-cell / pathway analysis (scVI, sspa) — P1-2.
#
# These heavyweight analysis packages are sensitive to anndata/zarr/scvi-tools
# version alignment and are only needed for the optional analysis pipeline.
# Install on demand:
#     pip install -r requirements-core.txt -r requirements-analysis.txt
#
# anndata<0.12 与 scvi-tools>=1.2.0 联合约束（审计 P1-1）：旧版 scvi-tools 依赖
# 被 anndata 0.11 移除的 SparseDataset，导致 import 失败；scvi-tools 1.2.0+ 已
# 改用 CSRDataset/CSCDataset。zarr<3 是 anndata 0.11.x 的要求。

-r requirements-core.txt

anndata>=0.10,<0.12
sspa>=0.2.0
scvi-tools>=1.2.0
zarr>=2,<3
hgvs==1.5.7
uniprot-id-mapper==1.1.5
UniProtMapper>=0.1

# TD-M05（2026-08-09）: scgpt 0.2.4 要求 scvi-tools<1.0，与上方
# scvi-tools>=1.2.0 冲突。项目代码不直接 import scgpt（davf.py 通过统一的
# EmbeddingLoaderBase 可选加载 Geneformer/scGPT 嵌入），因此本文件不声明
# scgpt；如需 scGPT 嵌入，请使用独立环境：
#     pip install "scvi-tools>=0.16,<1.0" scgpt==0.2.4
# 并将该环境用于 GenKI/DAVF 分析。全量混装 scgpt + scvi-tools>=1.2 会导致
# ``pip check`` 冲突，属已知取舍。

# GenKI 图扰动后端（src/integration/genki/perturbation.py 通过 importlib 延迟导入
# torch_geometric.data.Data；缺失时该 source backend 跳过，但应在此声明）。
torch-geometric>=2.3.0

# 文档构建（docs/conf.py 使用 sphinx.ext.autodoc/napoleon/viewcode/intersphinx）。
sphinx>=5.0.0

# GUI 已在 2026-07-05 取消，不再声明 Streamlit/Gradio 依赖。
