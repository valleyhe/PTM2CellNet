---
phase: 15-test-dependency-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
autonomous: true
requirements: [DEPS-01, DEPS-02, DEPS-03]

must_haves:
  truths:
    - "pip install -r requirements.txt installs anndata as a declared dependency"
    - "sspa is listed in requirements.txt with a comment marking it optional"
    - "lion-pytorch remains in requirements.txt as a real dependency (confirmed used by training code)"
  artifacts:
    - path: "requirements.txt"
      provides: "Complete dependency declarations"
      contains: "anndata"
  key_links:
    - from: "requirements.txt"
      to: "src/integration/genki/perturbation.py"
      via: "anndata import"
      pattern: "import anndata"
    - from: "requirements.txt"
      to: "src/training/lightning_module.py"
      via: "lion-pytorch conditional import"
      pattern: "from lion_pytorch import Lion"
---

<objective>
Declare missing dependencies and confirm existing ones in requirements.txt.

Purpose: Two dependencies (anndata, sspa) are imported by source code but missing from requirements.txt. One dependency (lion-pytorch) needs audit to confirm it is used. Fixing this prevents ImportError at runtime.

Output: Updated requirements.txt with all 3 dependency issues resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-test-dependency-fixes/15-CONTEXT.md

<interfaces>
<!-- Source: requirements.txt current structure -->
```text
# Core data
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# Deep learning
torch>=1.10.0
...
lion-pytorch>=0.0.7
...

# Bioinformatics
biopython>=1.79
hgvs==1.5.7
uniprot-id-mapper==1.1.5

...
```

Requirements.txt uses comment-section grouping. New dependencies should be added to the appropriate section.

<!-- Source: dependency evidence -->
```python
# src/integration/genki/perturbation.py:8
import anndata as ad

# src/integration/genki/reference_data.py:8
import anndata as ad

# src/analysis/pathway_integration.py:11
try:
    import sspa

# src/training/lightning_module.py:416
from lion_pytorch import Lion
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing dependencies and confirm lion-pytorch (DEPS-01, DEPS-02, DEPS-03, per D-06, D-07, D-08)</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt
    - src/integration/genki/perturbation.py
    - src/integration/genki/reference_data.py
    - src/analysis/pathway_integration.py
    - src/training/lightning_module.py
  </read_first>
  <action>
Edit `requirements.txt` to make 3 changes:

**Change 1 (DEPS-01, per D-06):** Add `anndata>=0.8.0` to the Bioinformatics section (after `uniprot-id-mapper==1.1.5`). This is a required dependency — imported directly (no try/except) in `src/integration/genki/perturbation.py` line 8 and `src/integration/genki/reference_data.py` line 8.

**Change 2 (DEPS-02, per D-07):** Add `sspa>=0.2.0  # optional - pathway enrichment analysis` to the Bioinformatics section (after `anndata`). The comment marks it as optional since the code uses `try: import sspa` with a fallback. Keep it in requirements.txt (not extras) with the comment annotation.

**Change 3 (DEPS-03, per D-08):** Keep `lion-pytorch>=0.0.7` in the Deep learning section as-is. Confirmed used in `src/training/lightning_module.py` line 416: `from lion_pytorch import Lion` with try/except fallback to AdamW. It IS a real dependency. No change needed.

The final Bioinformatics section should read:
```text
# Bioinformatics
biopython>=1.79
hgvs==1.5.7
uniprot-id-mapper==1.1.5
anndata>=0.8.0
sspa>=0.2.0  # optional - pathway enrichment analysis
```
  </action>
  <verify>
    <automated>cd /home/scu/PTM2CellNet && grep -c "anndata" requirements.txt</automated>
    Expected: 1 (anndata line added)
    <automated>cd /home/scu/PTM2CellNet && grep -c "sspa" requirements.txt</automated>
    Expected: 1 (sspa line added)
    <automated>cd /home/scu/PTM2CellNet && grep "lion-pytorch" requirements.txt</automated>
    Expected: lion-pytorch>=0.0.7 still present
  </verify>
  <acceptance_criteria>
    - `grep "anndata" requirements.txt` returns a line containing `anndata>=0.8.0`
    - `grep "sspa" requirements.txt` returns a line containing `sspa>=0.2.0` with `# optional` comment
    - `grep "lion-pytorch" requirements.txt` returns `lion-pytorch>=0.0.7` (unchanged)
    - `anndata` line is in the Bioinformatics section (after `uniprot-id-mapper`)
    - `sspa` line is after `anndata` in the Bioinformatics section
    - No other lines in requirements.txt are modified
  </acceptance_criteria>
  <done>
- `anndata>=0.8.0` added to Bioinformatics section in requirements.txt
- `sspa>=0.2.0` added as optional to Bioinformatics section in requirements.txt
- `lion-pytorch>=0.0.7` confirmed present and kept (used by lightning_module.py)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirements.txt -> runtime | Missing dependencies cause ImportError at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | D | requirements.txt | accept | Dependency declaration only, no runtime behavior change |
</threat_model>

<verification>
```bash
cd /home/scu/PTM2CellNet
# Verify all 3 dependency changes
grep "anndata" requirements.txt
grep "sspa" requirements.txt
grep "lion-pytorch" requirements.txt
# Verify requirements.txt is valid (no syntax errors)
pip check 2>/dev/null || true
```
</verification>

<success_criteria>
- `requirements.txt` contains `anndata>=0.8.0`
- `requirements.txt` contains `sspa>=0.2.0` with optional comment
- `requirements.txt` still contains `lion-pytorch>=0.0.7`
- No other lines in requirements.txt are changed
</success_criteria>

<output>
After completion, create `.planning/phases/15-test-dependency-fixes/15-02-SUMMARY.md`
</output>
