# Core runtime for minimal training/inference/API (P1-2).
#
# This is the smallest dependency set that lets a clean environment run the
# CNN/Transformer E2E loop:
#     pip install -r requirements-core.txt
#     python scripts/train.py --config configs/smoke/cnn_cpu.yaml ...
#     python scripts/predict.py --model outputs/.../best_model.pt ...
#
# Heavy / CUDA-sensitive capabilities (pretrained LMs, Mamba, scVI/analysis)
# live in their own requirements-*.txt files and are NOT pulled in by default.
# The Dockerfile installs only this core set for a lean production image.

# Core data
# D1 (2026-08-16): numpy<2 上限放宽至 <3 —— lock (numpy==2.4.3) 与 SSH_unit
# 验证环境实测 numpy 2.4.3 + torch 2.4.1 下 1919 测试全绿；core 与 lock 重新一致。
numpy>=1.24,<3
pandas>=1.3.0
scipy>=1.7.0

# Deep learning
torch>=2.0,<3
# Machine learning
scikit-learn>=1.0.0
torchmetrics>=0.11.0
networkx>=2.6.0

# Bioinformatics (lightweight)
biopython>=1.79
# MinHash+LSH 近似近邻用于大规模同源拆分（src/data/homology_splitter.py）。
# 缺失时回退稠密路径并 warn，但应作为核心依赖声明以支持完整功能。
datasketch>=1.5.0

# Visualization
matplotlib>=3.4.0
seaborn>=0.11.0

# Config & tools
pyyaml>=5.4.0
tqdm>=4.62.0
h5py>=3.6.0
requests>=2.31.0  # N19: >=2.31 修复 CVE-2023-32681
typing_extensions>=4.0.0
# setuptools<81 keeps the ``pkg_resources`` module available; it was removed in
# setuptools 81, which several scientific packages still import at runtime.
setuptools>=68.0,<81

# Web API (FastAPI service)
fastapi>=0.100.0  # N19: 下限对齐 setup.py，避免解析进含 CVE 旧版
uvicorn>=0.20.0  # N19: 下限对齐 setup.py
pydantic>=1.10,<3
# starlette is a FastAPI dependency; pin explicitly for reproducibility
starlette>=0.40.0  # N19: >=0.40 修复 CVE-2024-47874

# joblib used by src/data/homology_splitter.py for parallel processing
joblib>=1.2.0
# safetensors used by src/utils/checkpoint_utils.py and src/data/validation.py
safetensors>=0.3.0

# anndata is required by src/integration/genki/ modules but is declared in
# requirements-analysis.txt due to its heavyweight dependency chain (zarr, h5py, etc.).
# Install -r requirements-analysis.txt when using GenKI/scVI features.

# sspa is required by src/analysis/pathway_integration.py but is declared in
# requirements-analysis.txt. Install -r requirements-analysis.txt for KEGG/Reactome support.
