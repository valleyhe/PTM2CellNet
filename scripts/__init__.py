"""Scripts package for PTM2CellNet.

Provides the package identity required by ``setup.py`` console_scripts
(``scripts.train:main`` / ``scripts.predict:main``) and by the in-repo
reverse imports from ``src.analysis.ptm_smoke``. ``tools/`` and
``experimental/`` intentionally stay out of the distribution (development
helpers, not installed entry points).
"""
