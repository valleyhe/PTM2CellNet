# ESM-2 35M 预训练模型配置
# 使用ESM-2 35M模型作为序列编码器（中等版本，更多参数）

model:
  encoder_type: "esm2_35M"
  freeze_encoder: false
  num_classes: 2
  dropout: 0.1
  pretrained_cache_dir: null

training:
  max_epochs: 30
  batch_size: 8
  learning_rate: 5.0e-5
  weight_decay: 0.01
  optimizer: "adamw"
  scheduler: "cosine"

  accelerator: "gpu"
  devices: 1
  precision: "32"
  accumulate_grad_batches: 2

  early_stopping:
    enabled: true
    patience: 5
    monitor: "val_loss"
    mode: "min"

  checkpoint:
    enabled: true
    save_top_k: 3
    monitor: "val_loss"
    mode: "min"

  logger:
    type: "tensorboard"
    save_dir: "outputs/logs"
    name: "esm2_35m"

data:
  max_sequence_length: 1024
  num_workers: 4
  pin_memory: true
  valid_amino_acids: "ACDEFGHIKLMNPQRSTVWY"
  ptm_types:
    - "phosphorylation"
    - "acetylation"
    - "methylation"
    - "ubiquitination"
    - "sumoylation"

paths:
  outputs_root: "outputs"
  outputs_models: "outputs/models/esm2_35m"
